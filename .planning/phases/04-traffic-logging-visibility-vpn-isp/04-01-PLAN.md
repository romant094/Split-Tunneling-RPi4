---
phase: "04"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/routing.sh
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every new LAN-to-VPN connection is logged to journald with prefix [VPN]"
    - "Every new LAN-to-ISP connection is logged to journald with prefix [ISP]"
    - "Log rules fire only on connection initiation (--state NEW), not per-packet"
    - "Log rate is capped at 10/min burst 20 to protect against flood from streaming/gaming devices"
    - "routing.sh remains idempotent — re-running it does not add duplicate LOG rules"
    - "LOG rules are removed during the flush stage and re-added on rebuild"
  artifacts:
    - path: "scripts/routing.sh"
      provides: "Split-tunnel routing + NAT + LOG rules (all in one script)"
      contains: "LOG --log-prefix"
  key_links:
    - from: "scripts/routing.sh Stage 7 (iptables MASQUERADE)"
      to: "new Stage 7b (iptables LOG rules)"
      via: "ordered after MASQUERADE in FORWARD chain"
      pattern: "iptables.*FORWARD.*LOG.*log-prefix"
---

<objective>
Extend scripts/routing.sh with iptables LOG rules on the FORWARD chain that tag every new
LAN connection with [VPN] or [ISP] based on the outbound interface (awg0 vs eth0).

Purpose: Provides the raw per-connection routing decision data that vpn-status.sh (Plan 04-02)
will query from journald. Without these LOG entries there is no visibility into which path
each device's connections take.

Output: scripts/routing.sh with two new iptables LOG rules that follow the existing
iptables -C idempotency pattern, participate in the flush-and-rebuild lifecycle, and
rate-limit to prevent syslog flood.
</objective>

<execution_context>
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.planning/ROADMAP.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.planning/phases/04-traffic-logging-visibility-vpn-isp/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add iptables LOG rules to routing.sh</name>
  <files>scripts/routing.sh</files>

  <read_first>
    - scripts/routing.sh — read the entire file before touching it; understand the existing
      Stage 3 flush block and the Stage 7 iptables MASQUERADE block; the LOG rules follow
      the same iptables -C guard pattern used in Stage 7
  </read_first>

  <action>
    Make three targeted additions to scripts/routing.sh:

    1. FLUSH BLOCK — inside Stage 3 (after the two existing `ip route del` lines), add LOG
       rule removal using iptables -C before -D (matching the vpn-rollback.sh pattern for
       safe removal):

         iptables -C FORWARD -o "${VPN_IFACE}" -m state --state NEW -m limit --limit 10/min --limit-burst 20 -j LOG --log-prefix "[VPN] " --log-level 6 2>/dev/null && \
             iptables -D FORWARD -o "${VPN_IFACE}" -m state --state NEW -m limit --limit 10/min --limit-burst 20 -j LOG --log-prefix "[VPN] " --log-level 6 || true
         iptables -C FORWARD -o eth0 -m state --state NEW -m limit --limit 10/min --limit-burst 20 -j LOG --log-prefix "[ISP] " --log-level 6 2>/dev/null && \
             iptables -D FORWARD -o eth0 -m state --state NEW -m limit --limit 10/min --limit-burst 20 -j LOG --log-prefix "[ISP] " --log-level 6 || true

       Add a log() call before: "Stage 3: Flushing LOG rules (if present)..."
       These lines go at the START of Stage 3, before the ip route flush lines, so the
       iptables state is clean before routes are rebuilt.

    2. NEW STAGE 7b — add immediately after the closing `fi` of the existing NAT-02
       MASQUERADE block in Stage 7 (before Stage 8), with a log() separator line:

         # ─── Stage 7b: iptables LOG rules (D-01–D-05) ──────────────────────────────────
         # D-01: FORWARD chain only (LAN device traffic through RPi; not RPi's own OUTPUT traffic)
         # D-02: --state NEW — log connection initiations only, not every packet in the session
         # D-03: --limit 10/min --limit-burst 20 — rate cap to prevent flood from streaming/gaming
         # D-04: iptables -C idempotency guard before every -A (mirrors Stage 7 pattern)
         # D-05: --log-level 6 → syslog info → journald; no separate log file needed

         log "Stage 7b: Configuring iptables LOG rules (D-01–D-05)..."

         if iptables -C FORWARD -o "${VPN_IFACE}" -m state --state NEW -m limit --limit 10/min --limit-burst 20 -j LOG --log-prefix "[VPN] " --log-level 6 2>/dev/null; then
             log "LOG rule [VPN] on ${VPN_IFACE}: already present (no change)"
         else
             iptables -A FORWARD -o "${VPN_IFACE}" -m state --state NEW -m limit --limit 10/min --limit-burst 20 -j LOG --log-prefix "[VPN] " --log-level 6
             log "LOG rule [VPN] on ${VPN_IFACE}: added"
         fi

         if iptables -C FORWARD -o eth0 -m state --state NEW -m limit --limit 10/min --limit-burst 20 -j LOG --log-prefix "[ISP] " --log-level 6 2>/dev/null; then
             log "LOG rule [ISP] on eth0: already present (no change)"
         else
             iptables -A FORWARD -o eth0 -m state --state NEW -m limit --limit 10/min --limit-burst 20 -j LOG --log-prefix "[ISP] " --log-level 6
             log "LOG rule [ISP] on eth0: added"
         fi

    3. UPDATE the Stage 8 log summary line (around line 183) to include log rules in the
       final status output:
         log "  iptables LOG:    [VPN] on ${VPN_IFACE}, [ISP] on eth0 (NEW only, 10/min limit)"
       Insert this immediately after the existing "iptables rules:" summary log line.

    The iptables -C check for LOG rules must use the IDENTICAL flags as the -A command —
    every flag, in the same order — or iptables will report "No chain/target/match by that
    name" and treat it as absent. Use ${VPN_IFACE} (not hardcoded "awg0") for the VPN rule
    to stay consistent with the existing MASQUERADE pattern.

    Do NOT use fenced code blocks in this action. No hardcoded interface names in the VPN
    LOG rule — always use ${VPN_IFACE}.
  </action>

  <verify>
    <automated>
      bash -n scripts/routing.sh
      grep -c 'LOG --log-prefix "\[VPN\]"' scripts/routing.sh
      grep -c 'LOG --log-prefix "\[ISP\]"' scripts/routing.sh
      grep -c '\-\-limit 10/min \-\-limit-burst 20' scripts/routing.sh
      grep -c '\-\-state NEW' scripts/routing.sh
    </automated>
  </verify>

  <acceptance_criteria>
    - bash -n scripts/routing.sh exits 0 (no syntax errors)
    - grep -c 'LOG --log-prefix "\[VPN\]"' scripts/routing.sh returns 3 (flush -D check, -A check, -A add)
    - grep -c 'LOG --log-prefix "\[ISP\]"' scripts/routing.sh returns 3 (flush -D check, -A check, -A add)
    - grep -c '\-\-limit 10/min \-\-limit-burst 20' scripts/routing.sh returns at least 4 (both rules, both guard+add)
    - grep -c '\-\-state NEW' scripts/routing.sh returns at least 4 (both rules, both guard+add)
    - grep 'Stage 7b' scripts/routing.sh exits 0 (new stage header present)
    - grep 'Stage 3' scripts/routing.sh shows LOG rule removal lines before ip route flush lines
    - No occurrence of hardcoded "awg0" inside the new LOG rule lines — only ${VPN_IFACE}
    - The LOG rule guard uses -C with identical flags to the -A command (copy-paste parity)
  </acceptance_criteria>

  <done>scripts/routing.sh extended with two iptables LOG rules (D-01–D-05): FORWARD chain,
  --state NEW, --limit 10/min --limit-burst 20, --log-level 6, [VPN] and [ISP] prefixes;
  idempotency guards match the Stage 7 MASQUERADE pattern; flush block clears old LOG rules
  before rebuild; script passes bash -n.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kernel → syslog | iptables LOG writes kernel messages; no userspace input involved |
| LAN devices → FORWARD chain | untrusted LAN traffic crosses RPi FORWARD; LOG rules observe but do not filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | iptables LOG on FORWARD | mitigate | --limit 10/min --limit-burst 20 rate cap prevents syslog flood from streaming/gaming devices (D-03) |
| T-04-02 | Information Disclosure | syslog LOG entries contain SRC/DST IPs | accept | logs stored in journald on RPi only; no external export; low-value local data |
| T-04-03 | Tampering | routing.sh idempotency during concurrent runs | accept | same flush-and-rebuild pattern as existing MASQUERADE rules; operator does not run concurrent instances |
</threat_model>

<verification>
On RPi after deploy (Plan 04-03):
  sudo iptables -L FORWARD -n -v | grep LOG
  # expect: two LOG rules — one with [VPN], one with [ISP]

  sudo journalctl -k -f
  # generate LAN traffic; expect kernel lines with [VPN] or [ISP] prefix + SRC= DST= fields

  sudo /etc/routing.sh --no-update && sudo iptables -L FORWARD -n -v | grep LOG
  # re-run must not duplicate LOG rules (idempotency check)
</verification>

<success_criteria>
- scripts/routing.sh passes bash -n with no syntax errors
- Stage 7b present with two iptables LOG rules using [VPN] and [ISP] prefixes
- Stage 3 flush block removes LOG rules before rebuild (checked with iptables -C before -D)
- --limit 10/min --limit-burst 20 present on both rules
- --state NEW present on both rules
- --log-level 6 present on both rules
- ${VPN_IFACE} used (not hardcoded "awg0") in the VPN LOG rule
</success_criteria>

<output>
Create .planning/phases/04-traffic-logging-visibility-vpn-isp/04-01-SUMMARY.md when done
</output>
