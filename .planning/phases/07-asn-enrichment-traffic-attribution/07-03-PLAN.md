---
phase: "07-asn-enrichment-traffic-attribution"
plan: "03"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - scripts/watch-routes.py
  - deploy.sh
autonomous: true
requirements: []

must_haves:
  truths:
    - "scripts/watch-routes.py appends ' | {org}' at the end of each output line whenever the destination IP has a known org"
    - "When ASN is unresolved (cache miss + network in flight, or Cymru unreachable), the line is printed immediately without the ' | org' suffix — the real-time stream never blocks"
    - "ASN lookups in watch-routes.py run in a background thread; concurrent access to the shared cache dict is guarded by threading.Lock"
    - "deploy.sh defines ASN_LOOKUP_LOCAL=scripts/asn-lookup.py, ASN_LOOKUP_REMOTE=/etc/asn-lookup.py, ASN_LOOKUP_TMP=/tmp/asn-lookup.py.tmp"
    - "deploy.sh preflight (Stage 1) checks ASN_LOOKUP_LOCAL exists before any SSH operation"
    - "deploy.sh Stage 24 SCPs scripts/asn-lookup.py → /tmp staging → sudo mv → chmod +x → chown root:root, mirroring Stage 19/20 pattern"
    - "deploy.sh TOTAL_STAGES bumped from 23 to 24 (Pitfall 7) so progress display reads [24/24] for the final stage"
    - "Stage 23 (NEW) deploys asn-lookup.py to /etc/asn-lookup.py; Stage 24 (renumbered from prior Stage 23) activates routes via routing.sh — routing activation remains the LAST runtime stage"
    - "deploy.sh final-summary block prints a 'PHASE 7: /etc/asn-lookup.py (chmod +x, root:root)' line"
  artifacts:
    - path: "scripts/watch-routes.py"
      provides: "Live tail with inline org enrichment via background thread"
      contains: "threading"
    - path: "scripts/watch-routes.py"
      contains: "_asn_cache"
    - path: "scripts/watch-routes.py"
      contains: "asn-lookup.py"
    - path: "deploy.sh"
      provides: "asn-lookup.py deploy stage + TOTAL_STAGES bump"
      contains: "ASN_LOOKUP_REMOTE"
    - path: "deploy.sh"
      contains: "TOTAL_STAGES=24"
  key_links:
    - from: "scripts/watch-routes.py format_line()"
      to: "_asn_cache dict"
      via: "lock.acquire → read dict → release; on miss start background thread"
      pattern: "_asn_cache"
    - from: "scripts/watch-routes.py background thread"
      to: "/etc/asn-lookup.py (subprocess)"
      via: "subprocess.run(['python3','/etc/asn-lookup.py'], input=ip+'\\n', ...)"
      pattern: "asn-lookup\\.py"
    - from: "deploy.sh Stage 24"
      to: "scripts/asn-lookup.py"
      via: "scp + sudo mv + chmod +x + chown"
      pattern: "ASN_LOOKUP_LOCAL.*ASN_LOOKUP_TMP|scp.*asn-lookup"
---

<objective>
Two tightly-coupled deliverables: (1) Extend `scripts/watch-routes.py` to enrich each live `[VPN]/[ISP]` line with `| {org}` via a background-thread lookup against `/etc/asn-lookup.py`, never blocking the journalctl stream. (2) Extend `deploy.sh` with a new Stage 24 that ships `scripts/asn-lookup.py` to `/etc/asn-lookup.py` on the RPi using the established `SCP → sudo mv → chmod +x → chown root:root` pattern.

These ship together because the streaming-enrichment change is meaningless without the file being deployed, and Plan 03 owns both files exclusively (no overlap with Plan 02). They are also independent of Plan 02's `vpn-status.sh` work, so wave 2 runs both plans in parallel.

Purpose: Live traffic monitoring shows org attribution inline without ever pausing the tail; deploy pipeline installs the shared helper script on RPi alongside vpn-status.sh and watch-routes.py.

Output: `scripts/watch-routes.py` with a module-level `_asn_cache: dict[str, dict|None]`, a `threading.Lock`, a background-thread `lookup_async(ip)` function, and `format_line()` updated to append ` | {org}` from the cache when present. `deploy.sh` with new ASN_LOOKUP_* variables, a preflight check, `TOTAL_STAGES=24`, and a new Stage 24 block ahead of the routing-activation stage.
</objective>

<execution_context>
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.planning/PROJECT.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.planning/ROADMAP.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.planning/STATE.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.planning/phases/07-asn-enrichment-traffic-attribution/07-CONTEXT.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.planning/phases/07-asn-enrichment-traffic-attribution/07-RESEARCH.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.planning/phases/07-asn-enrichment-traffic-attribution/07-01-PLAN.md

<interfaces>
<!-- asn-lookup.py contract (locked by Plan 07-01) -->

Subprocess invocation from watch-routes.py:
  subprocess.run(
      ["python3", "/etc/asn-lookup.py"],
      input=ip + "\n",
      capture_output=True,
      text=True,
      timeout=5.0,
  )
  → stdout is one JSON line: {"<ip>": {"asn": "...", "org": "..."}}
  → exit code is always 0 on success; treat any non-zero or empty stdout as "no result"

<!-- Existing watch-routes.py structure (from source — DO NOT re-derive in executor) -->

Module-level globals (line 24, 26):
  _dns_cache: dict[str, str] = {}
  socket.setdefaulttimeout(2.0)

format_line signature (line 61):
  def format_line(ts, tag, src, dst, proto, dpt, no_dns) -> str

Current return (line 72):
  return f"{ts} [{tag}] {src} → {dst_part} {port_part}"

Main loop (lines 128–151) iterates `proc.stdout` and calls `format_line(...)` → prints.

<!-- Existing deploy.sh structure (from source — DO NOT re-derive in executor) -->

TOTAL_STAGES=23  (line 66 — change to 24)

Path variable block (lines 53–58 are the closest analogs):
  VPN_STATUS_LOCAL="scripts/vpn-status.sh"
  VPN_STATUS_REMOTE="/etc/vpn-status.sh"
  VPN_STATUS_TMP="/tmp/vpn-status.sh.tmp"
  WATCH_ROUTES_LOCAL="scripts/watch-routes.py"
  WATCH_ROUTES_REMOTE="/etc/watch-routes.py"
  WATCH_ROUTES_TMP="/tmp/watch-routes.py.tmp"

New variables to add (after line 58):
  ASN_LOOKUP_LOCAL="scripts/asn-lookup.py"
  ASN_LOOKUP_REMOTE="/etc/asn-lookup.py"
  ASN_LOOKUP_TMP="/tmp/asn-lookup.py.tmp"

Preflight check pattern (lines 135–143):
  if [[ ! -f "$VPN_STATUS_LOCAL" ]]; then
      echo "ERROR: $VPN_STATUS_LOCAL not found — run from the repo root" >&2
      exit 1
  fi

Stage 19 deploy pattern (lines 342–346 — the exact template to mirror for Stage 24):
  echo "[19/${TOTAL_STAGES}] Deploying vpn-status.sh to ${SSH_HOST}:${VPN_STATUS_REMOTE}..."
  scp -o BatchMode=yes "${VPN_STATUS_LOCAL}" "${SSH_HOST}:${VPN_STATUS_TMP}"
  ssh -o BatchMode=yes "${SSH_HOST}" "sudo mv ${VPN_STATUS_TMP} ${VPN_STATUS_REMOTE} && sudo chmod +x ${VPN_STATUS_REMOTE} && sudo chown root:root ${VPN_STATUS_REMOTE}"
  echo "       vpn-status.sh deployed (chmod +x, root:root)."

Current Stage 23 (lines 371–374):
  echo "[23/${TOTAL_STAGES}] Activating routes via routing.sh on ${SSH_HOST}..."
  ssh -o BatchMode=yes "${SSH_HOST}" "sudo ${ROUTING_SH_REMOTE}"

Final summary block (lines 380–399): contains "PHASE 4:", "PHASE 5:", "PHASE 6:" lines — add "PHASE 7:" line.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend scripts/watch-routes.py with background-thread ASN enrichment</name>
  <files>scripts/watch-routes.py</files>
  <read_first>
    - scripts/watch-routes.py — FULL FILE; specifically the DNS cache pattern (lines 21–43), _LOG_RE regex (lines 50–58), format_line() (lines 61–72), main() loop (lines 108–155). The executor needs the existing module-global cache pattern, the resolve() function shape, and the print(..., flush=True) pattern.
    - .planning/phases/07-asn-enrichment-traffic-attribution/07-01-PLAN.md — `<interfaces>` block (asn-lookup.py subprocess contract)
    - .planning/phases/07-asn-enrichment-traffic-attribution/07-RESEARCH.md — Pattern 3 (background thread approach), Pattern 4 alt (subprocess with timeout), Pitfall 8 (hyphenated filename → must use subprocess, NEVER `import asn-lookup`), Code Examples §"Appending org to watch-routes.py format_line()"
    - .planning/phases/07-asn-enrichment-traffic-attribution/07-CONTEXT.md — D-04, D-08
  </read_first>
  <behavior>
    - Test 1: When `_asn_cache["8.8.8.8"] == {"asn":"15169","org":"GOOGLE, US"}` is pre-populated, `format_line("2026-05-21T11:36:21","VPN","192.168.1.175","8.8.8.8","TCP","443", no_dns=True)` returns a string ending with ` | GOOGLE, US`.
    - Test 2: When `_asn_cache["1.1.1.1"]` is `None` (lookup in progress), `format_line(...,"1.1.1.1",...)` returns a string that does NOT contain ` | ` followed by an org name (just prints without suffix).
    - Test 3: When `_asn_cache["1.1.1.1"]` is `{}` (lookup completed, no result), `format_line(...,"1.1.1.1",...)` returns a string without the ` | org` suffix.
    - Test 4: When `dst` is absent from `_asn_cache`, calling `format_line` triggers `lookup_async("1.1.1.1")` exactly once. A subsequent `format_line` call for the same IP while the first thread is still running does NOT spawn a second thread (verified by patching threading.Thread).
    - Test 5: `lookup_async("8.8.8.8")` with the subprocess stubbed to return `'{"8.8.8.8":{"asn":"15169","org":"GOOGLE, US"}}\n'` populates `_asn_cache["8.8.8.8"] == {"asn":"15169","org":"GOOGLE, US"}`.
    - Test 6: `lookup_async("9.9.9.9")` with subprocess.TimeoutExpired or non-zero exit populates `_asn_cache["9.9.9.9"] == {}` (sentinel: lookup completed, no result — prevents re-querying).
    - Test 7: Concurrent calls `lookup_async("X")` from two threads result in exactly one subprocess invocation (verified via `mock.call_count`).
    - Test 8: Existing flags (`--src`, `--no-dns`, `--tag`) still parse and apply identically. Adding `--no-asn` flag (opt-out for users who want pure no-network mode) skips all ASN lookups; `_asn_cache` is never written; line never has `| org` suffix.
    - Test 9: `format_line` signature is backward-compatible — existing positional call from `main()` (lines 150) still works after change (either default-arg approach or main() updated to pass new arg).
  </behavior>
  <action>
    Modify `scripts/watch-routes.py`. Concrete required changes:

    1. Imports block (line 14–19): add `import json` and `import threading`. Do NOT add `from asn_lookup import ...` — Pitfall 8 (hyphen breaks import; must use subprocess).

    2. Module globals (after the existing `_dns_cache` block around line 24):
       - `_asn_cache: dict[str, dict | None] = {}`  — value `None` means "lookup in flight"; value `{}` means "lookup completed, no result"; value `{"asn":..., "org":...}` means "resolved".
       - `_asn_lock = threading.Lock()`
       - `ASN_LOOKUP_PATH = "/etc/asn-lookup.py"`
       - `ASN_SUBPROCESS_TIMEOUT = 5.0`

    3. Add a new function `lookup_async(ip: str) -> None`:
       - With `_asn_lock`: if `ip in _asn_cache`, return (already in flight or resolved); else set `_asn_cache[ip] = None` (sentinel: in flight).
       - Outside the lock, spawn a daemon `threading.Thread(target=_do_lookup, args=(ip,), daemon=True)` and `.start()` it.

    4. Add `_do_lookup(ip: str) -> None`:
       - Wrap whole body in `try / except Exception`.
       - Call `subprocess.run(["python3", ASN_LOOKUP_PATH], input=ip + "\n", capture_output=True, text=True, timeout=ASN_SUBPROCESS_TIMEOUT)`.
       - On success and non-empty stdout: `data = json.loads(proc.stdout)`; with `_asn_lock`: `_asn_cache[ip] = data.get(ip) or {}`.
       - On `subprocess.TimeoutExpired`, `FileNotFoundError`, `json.JSONDecodeError`, or non-zero return code: with `_asn_lock`: `_asn_cache[ip] = {}` (completed, no result — never re-query this run).

    5. Modify `format_line` (line 61):
       - Add a new keyword-only parameter: `enable_asn: bool = True`.
       - At the END of the function, before the `return` (line 72), insert:
         ```
         if enable_asn:
             with _asn_lock:
                 cached = _asn_cache.get(dst, "__missing__")
             if cached == "__missing__":
                 lookup_async(dst)
             elif cached and cached.get("org"):
                 return f"{ts} [{tag}] {src} → {dst_part} {port_part} | {cached['org']}"
         return f"{ts} [{tag}] {src} → {dst_part} {port_part}"
         ```
       - Rationale: cache lookup needs three states (missing / in-flight / resolved-empty / resolved-org); the sentinel `"__missing__"` avoids racing with concurrent thread writes that may set the key to `None` between our `.get(dst)` and our suffix decision.

    6. Argparse (around line 75–105): add a new mutually-non-exclusive flag:
       ```
       parser.add_argument("--no-asn", action="store_true", default=False,
                           help="Skip background ASN/org enrichment; print lines as before.")
       ```

    7. main() (lines 108–155): when calling `format_line(...)` on line 150, pass `enable_asn=not args.no_asn`.

    8. Update the module docstring (lines 2–13) to mention the new behavior and the asn-lookup.py subprocess dependency, plus the `--no-asn` flag.

    Test file: create `tests/test_watch_routes_asn.py` covering Tests 1–9 above. For subprocess mocking, use `unittest.mock.patch("subprocess.run")` with a `MagicMock` returning a configurable stdout. For the threading test (Test 7), patch `threading.Thread` to record args and assert call_count. Pre-populate `_asn_cache` for Tests 1–3 by direct dict mutation in the test setUp. Reset `_asn_cache` and `_asn_lock` between tests in tearDown.

    Do NOT modify `_LOG_RE`, the journalctl spawn, or the DNS resolve flow. Those are out of scope.
  </action>
  <verify>
    <automated>
      cd /Users/antonromankov/Projects/my-projects/split-tunneling-v2 &amp;&amp; \
      python3 -c "import ast; ast.parse(open('scripts/watch-routes.py').read())" &amp;&amp; \
      python3 -m pytest tests/test_watch_routes_asn.py -x -v &amp;&amp; \
      grep -q "_asn_cache" scripts/watch-routes.py &amp;&amp; \
      grep -q "threading" scripts/watch-routes.py &amp;&amp; \
      grep -q "asn-lookup.py" scripts/watch-routes.py &amp;&amp; \
      grep -q -- "--no-asn" scripts/watch-routes.py &amp;&amp; \
      ! grep -E "^import asn-lookup|^from asn-lookup" scripts/watch-routes.py &amp;&amp; \
      python3 scripts/watch-routes.py --help | grep -q -- "--no-asn"
    </automated>
  </verify>
  <acceptance_criteria>
    - `python3 -c "import ast; ast.parse(open('scripts/watch-routes.py').read())"` succeeds
    - File contains literal `_asn_cache`
    - File contains literal `threading` (imported and used for Lock + Thread)
    - File contains literal `asn-lookup.py` (the subprocess target path)
    - File does NOT contain `import asn-lookup` or `from asn-lookup` (Pitfall 8 — hyphen breaks imports)
    - `python3 scripts/watch-routes.py --help` exits 0 and prints `--no-asn` in usage
    - `tests/test_watch_routes_asn.py` exists and passes all 9 tests under pytest
    - `format_line(..., enable_asn=False)` returns the original string without `| org` suffix (regression: existing behavior preserved when ASN disabled)
    - Background thread is a daemon thread (`daemon=True`) — prevents hanging on Ctrl-C
  </acceptance_criteria>
  <done>Live `sudo watch-routes.py` tail enriches each line with ` | {org}` without stalling on Cymru network I/O; failure / opt-out fall back to the pre-Phase-7 line format.</done>
</task>

<task type="auto">
  <name>Task 2: Extend deploy.sh with Stage 24 (asn-lookup.py deploy) and TOTAL_STAGES bump</name>
  <files>deploy.sh</files>
  <read_first>
    - deploy.sh — FULL FILE; specifically the path variable block (lines 32–66), the preflight `[[ ! -f ... ]]` check pattern (lines 95–145), Stage 19/20 deploy block (lines 342–352, the exact template to mirror), Stage 23 routing activation (lines 371–374), the final summary block (lines 380–399 — contains "PHASE 4:", "PHASE 5:", "PHASE 6:" lines that need a "PHASE 7:" sibling).
    - .planning/phases/07-asn-enrichment-traffic-attribution/07-RESEARCH.md — Pitfall 7 (TOTAL_STAGES off-by-one — MUST bump 23→24)
    - .planning/phases/07-asn-enrichment-traffic-attribution/07-CONTEXT.md — D-05 (deploy stage added)
  </read_first>
  <action>
    Modify `deploy.sh`. Concrete required changes:

    1. Path variables block (insert after line 58, alongside WATCH_ROUTES_*):
       ```
       ASN_LOOKUP_LOCAL="scripts/asn-lookup.py"
       ASN_LOOKUP_REMOTE="/etc/asn-lookup.py"
       ASN_LOOKUP_TMP="/tmp/asn-lookup.py.tmp"
       ```

    2. TOTAL_STAGES (line 66): change `TOTAL_STAGES=23` → `TOTAL_STAGES=24` (Pitfall 7).

    3. Preflight check (in the existing block around lines 135–143, alongside the VPN_STATUS_LOCAL and WATCH_ROUTES_LOCAL checks):
       ```
       if [[ ! -f "$ASN_LOOKUP_LOCAL" ]]; then
           echo "ERROR: $ASN_LOOKUP_LOCAL not found — run from the repo root" >&2
           exit 1
       fi
       ```

    4. Insertion order decision — REQUIRED: insert the new Stage 24 BEFORE the current Stage 23 routing activation, and renumber the current Stage 23 to Stage 24. Rationale: routing.sh activation must remain the LAST runtime stage so the final summary block sees both file deploys complete. After the change the order is:
       - Stage 22 (existing): NM dispatcher deploy
       - Stage 23 (NEW): Deploy asn-lookup.py
       - Stage 24 (renumbered from prior Stage 23): Activate routes via routing.sh

       Implementation:
       a. Locate the existing Stage 23 block (lines 371–374) and renumber its echo from `[23/${TOTAL_STAGES}]` to `[24/${TOTAL_STAGES}]`; update the comment header from `# ─── Stage 23: Activate Phase 5 routes via routing.sh` to `# ─── Stage 24: Activate Phase 5 routes via routing.sh`.
       b. Insert directly above it (before the renumbered routing-activation block) the new Stage 23 block, mirroring Stage 19 (vpn-status.sh) verbatim:
          ```
          # ─── Stage 23: Deploy asn-lookup.py to RPi (Phase 7) ─────────────────────────
          echo "[23/${TOTAL_STAGES}] Deploying asn-lookup.py to ${SSH_HOST}:${ASN_LOOKUP_REMOTE}..."
          scp -o BatchMode=yes "${ASN_LOOKUP_LOCAL}" "${SSH_HOST}:${ASN_LOOKUP_TMP}"
          ssh -o BatchMode=yes "${SSH_HOST}" "sudo mv ${ASN_LOOKUP_TMP} ${ASN_LOOKUP_REMOTE} && sudo chmod +x ${ASN_LOOKUP_REMOTE} && sudo chown root:root ${ASN_LOOKUP_REMOTE}"
          echo "       asn-lookup.py deployed (chmod +x, root:root)."
          ```

    5. Final summary block (the multi-line `echo "..."` group around lines 384–399): add one new line alongside the existing "PHASE 4:" / "PHASE 5:" / "PHASE 6:" lines:
       ```
       echo "   PHASE 7: ${ASN_LOOKUP_REMOTE} (chmod +x, root:root — ASN/org enrichment helper)"
       ```
       Place it right after the existing `PHASE 6:` line for chronological order.

    6. Decisions-honored comment header (top of file, around the "Decisions honored:" block): append a new line:
       ```
       #   PHASE 7 D-05: asn-lookup.py deployed to /etc/asn-lookup.py via Stage 23 (same SCP→mv→chmod pattern as Stage 19/20)
       ```

    7. Do NOT modify the `vpn-rollback.sh` script in this plan — per RESEARCH.md "Project Constraints" the rollback does NOT need to remove asn-lookup.py (it is a read-only enrichment tool with no routing side effects). Note this in the new comment for traceability.

    Validation: run `bash -n deploy.sh` to confirm syntax. Ensure every existing `[N/${TOTAL_STAGES}]` echo from Stage 1 through Stage 22 is unchanged.
  </action>
  <verify>
    <automated>
      cd /Users/antonromankov/Projects/my-projects/split-tunneling-v2 &amp;&amp; \
      bash -n deploy.sh &amp;&amp; \
      grep -q '^TOTAL_STAGES=24$' deploy.sh &amp;&amp; \
      grep -q 'ASN_LOOKUP_LOCAL="scripts/asn-lookup.py"' deploy.sh &amp;&amp; \
      grep -q 'ASN_LOOKUP_REMOTE="/etc/asn-lookup.py"' deploy.sh &amp;&amp; \
      grep -q 'ASN_LOOKUP_TMP="/tmp/asn-lookup.py.tmp"' deploy.sh &amp;&amp; \
      grep -q '\[23/\${TOTAL_STAGES}\] Deploying asn-lookup.py' deploy.sh &amp;&amp; \
      grep -q '\[24/\${TOTAL_STAGES}\] Activating routes via routing.sh' deploy.sh &amp;&amp; \
      grep -q 'PHASE 7:' deploy.sh &amp;&amp; \
      grep -q 'sudo mv \${ASN_LOOKUP_TMP} \${ASN_LOOKUP_REMOTE} && sudo chmod +x \${ASN_LOOKUP_REMOTE} && sudo chown root:root \${ASN_LOOKUP_REMOTE}' deploy.sh &amp;&amp; \
      grep -c -E '^\[?[0-9]+/\$\{TOTAL_STAGES\}\]?' deploy.sh &amp;&amp; \
      ! grep -E '^\s*echo\s+"\[(0?[1-9]|1[0-9]|2[0-2])/' deploy.sh | grep -v "TOTAL_STAGES" &amp;&amp; \
      grep -c '\[[0-9]*/\${TOTAL_STAGES}\]' deploy.sh
    </automated>
  </verify>
  <acceptance_criteria>
    - `bash -n deploy.sh` exits 0 (syntax valid)
    - File contains exact literal line `TOTAL_STAGES=24` (Pitfall 7)
    - File contains all three new variable assignments: `ASN_LOOKUP_LOCAL="scripts/asn-lookup.py"`, `ASN_LOOKUP_REMOTE="/etc/asn-lookup.py"`, `ASN_LOOKUP_TMP="/tmp/asn-lookup.py.tmp"`
    - File contains a preflight `if [[ ! -f "$ASN_LOOKUP_LOCAL" ]]; then ... exit 1; fi` block
    - File contains the new Stage 23 deploy block with `[23/${TOTAL_STAGES}] Deploying asn-lookup.py` echo, exactly one `scp -o BatchMode=yes` line, and exactly one `ssh -o BatchMode=yes ... sudo mv ... && sudo chmod +x ... && sudo chown root:root` line
    - The routing-activation stage echo is renumbered to `[24/${TOTAL_STAGES}] Activating routes via routing.sh` (last runtime stage)
    - Final summary contains a `PHASE 7:` line referencing `${ASN_LOOKUP_REMOTE}`
    - Stage numbers 1 through 22 unchanged (no off-by-one drift on earlier stages — verified by `grep -c '\[[0-9]*/\${TOTAL_STAGES}\]' deploy.sh` returning the same per-stage count modulo the new line)
    - No reference to vpn-rollback.sh modification (out of scope per RESEARCH.md)
  </acceptance_criteria>
  <done>`./deploy.sh` runs end-to-end on a clean RPi: ships asn-lookup.py to `/etc/asn-lookup.py` in Stage 23, then activates routing in Stage 24, with progress display reading `[N/24]`. Plan 02's `vpn-status.sh --summary` and Task 1's enriched `watch-routes.py` both find `/etc/asn-lookup.py` available on the RPi.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| macOS → RPi (SCP/SSH) | Existing SSH+BatchMode trust; same channel used since Phase 1 |
| watch-routes.py (background thread) → /etc/asn-lookup.py (subprocess) | Same trust as Task 1 (Plan 02): subprocess input is an IP extracted from kernel iptables LOG (DST=<digits.dots>) |
| _asn_cache shared dict | Multi-threaded access guarded by _asn_lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Tampering | Cache dict concurrent write | mitigate | All writes to `_asn_cache` go through `with _asn_lock:`; sentinel `None` prevents duplicate-thread spawn |
| T-07-11 | Denial of Service | Background thread accumulation (one per missed IP) | mitigate | Daemon threads die with the process; lookup_async is idempotent (early return if key present); sentinel `{}` prevents re-spawn after completed-no-result |
| T-07-12 | Denial of Service | subprocess.run hang on /etc/asn-lookup.py | mitigate | `timeout=5.0` on subprocess.run; TimeoutExpired caught → cache set to `{}` so we never re-spawn for that IP this run |
| T-07-13 | Spoofing | Symlink attack on /tmp/asn-lookup.py.tmp | mitigate | Plan 01 / Plan 03 use `mktemp`-equivalent path under SSH user's tmp; `sudo mv` overwrites destination atomically; same pattern used since Phase 1 D-11 |
| T-07-14 | Elevation of Privilege | Stage 23 ssh command injection via ${ASN_LOOKUP_*} variables | mitigate | All three variables are repo-controlled string literals (not user input); identical pattern to Stages 19/20 which passed prior phase security review |
</threat_model>

<verification>
After both tasks complete:
- `python3 -m pytest tests/test_watch_routes_asn.py -x -v` passes
- `bash -n deploy.sh && bash -n scripts/vpn-status.sh && python3 -c "import ast; ast.parse(open('scripts/watch-routes.py').read())"` all succeed
- `grep -c '\[[0-9]*/\${TOTAL_STAGES}\]' deploy.sh` returns 25 (one for each of 24 stages plus one duplicate for the --no-run branch of Stage 11, matching the pre-change count + 1 for Stage 23-new)
- Smoke test (on RPi after deploy): `printf "8.8.8.8\n" | python3 /etc/asn-lookup.py` emits a JSON dict
- Smoke test (on RPi after deploy): `sudo /etc/watch-routes.py` runs without exception and prints lines (org may appear after 1–2 lines per Pattern 3 design)
</verification>

<success_criteria>
1. scripts/watch-routes.py: ` | {org}` appended when cache resolves; never blocks the journalctl tail
2. scripts/watch-routes.py: `_asn_cache` + `_asn_lock` + `lookup_async` + `_do_lookup` implemented; subprocess timeout 5s
3. scripts/watch-routes.py: `--no-asn` flag opts out of enrichment; existing flags preserved
4. tests/test_watch_routes_asn.py: 9 tests pass, no live Cymru / subprocess invocations
5. deploy.sh: TOTAL_STAGES=24 (Pitfall 7); ASN_LOOKUP_* paths declared; preflight check added
6. deploy.sh: new Stage 23 deploys asn-lookup.py with `chmod +x`, `chown root:root`; renumbered Stage 24 activates routing.sh
7. deploy.sh: `PHASE 7:` line in final summary
8. `bash -n deploy.sh` exit 0; no syntax regressions on prior stages
</success_criteria>

<output>
Create `.planning/phases/07-asn-enrichment-traffic-attribution/07-03-SUMMARY.md` when done
</output>
