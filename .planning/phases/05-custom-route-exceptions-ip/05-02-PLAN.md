---
phase: 05-custom-route-exceptions-ip
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - scripts/vpn-status.sh
  - deploy.sh
  - configs/white-list-extended.txt.example
  - .gitignore
autonomous: true
requirements: []
must_haves:
  truths:
    - "vpn-status.sh accepts --via=vpn and --via=isp flags and filters output to only matching rows"
    - "vpn-status.sh --via composes with --filter, --device, --last (any combination works without mutual exclusion)"
    - "vpn-status.sh --via with any value other than 'vpn' or 'isp' exits non-zero with a clear error"
    - "deploy.sh has a new stage that SCPs configs/white-list-extended.txt to /etc/white-list-extended.txt when the local file exists, and logs a skip line when it does not"
    - "deploy.sh Stage A (preflight) does NOT require configs/white-list-extended.txt — only the .example file (if checked at all) and the existing required files"
    - "deploy.sh final routing.sh activation stage runs without --no-update on the first Phase 5 deploy so /etc/white-list.txt is freshly downloaded under its new name"
    - "configs/white-list-extended.txt.example is committed and shows the expected one-CIDR-per-line format with a header comment"
    - "configs/white-list-extended.txt is in .gitignore"
    - "deploy.sh TOTAL_STAGES is updated to reflect the new stage (21 → 22)"
  artifacts:
    - path: "scripts/vpn-status.sh"
      provides: "Output filter by routing decision via --via=vpn|isp"
      contains: '--via='
    - path: "deploy.sh"
      provides: "Conditional deploy stage for white-list-extended.txt"
      contains: 'white-list-extended'
    - path: "deploy.sh"
      provides: "Updated stage count after inserting the new stage"
      contains: 'TOTAL_STAGES=22'
    - path: "configs/white-list-extended.txt.example"
      provides: "User-facing template showing CIDR-per-line format with header comment"
      min_lines: 5
    - path: ".gitignore"
      provides: "Ignore the user's actual exception file"
      contains: 'configs/white-list-extended.txt'
  key_links:
    - from: "scripts/vpn-status.sh argument parser"
      to: "output loop"
      via: "VIA variable; if non-empty, skip rows whose decision != VIA"
      pattern: '\[\[ -n "\$\{VIA\}" \]\]'
    - from: "deploy.sh new exception-file stage"
      to: "/etc/white-list-extended.txt on RPi"
      via: "scp -> sudo mv -> sudo chmod 644 -> sudo chown root:root, gated by local file presence"
      pattern: 'configs/white-list-extended.txt'
    - from: "deploy.sh final routing.sh activation stage"
      to: "/etc/routing.sh"
      via: "ssh ... sudo /etc/routing.sh (no --no-update on first deploy after rename)"
      pattern: 'sudo \$\{ROUTING_SH_REMOTE\}"$'
---

<objective>
Wire the user-facing surface of Phase 5: add the --via=vpn|isp filter to vpn-status.sh so the operator can discover which IPs are exiting via VPN (the discovery half of the "discover → exception" workflow), add a new deploy.sh stage that conditionally SCPs configs/white-list-extended.txt to /etc/white-list-extended.txt on the RPi when the local file is present, fix the post-rename --no-update bootstrap problem (Research Open Question #3 / Pitfall 2) by dropping --no-update from the final activation stage, and ship the committed configs/white-list-extended.txt.example template plus the .gitignore entry for the user's actual exception file.

Purpose: Plan 01 made the RPi side ready (routing.sh loads /etc/white-list-extended.txt when present, paths renamed to white-list.txt). Plan 02 ships the macOS deploy plumbing, the discovery tool flag, and the user-facing config template so the full Phase 5 workflow (sudo vpn-status.sh --via=vpn → add CIDRs locally → ./deploy.sh) becomes operational end-to-end.

Output: vpn-status.sh, deploy.sh, configs/white-list-extended.txt.example, .gitignore updated/created; Phase 5 ready for live verification on the RPi.
</objective>

<execution_context>
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-custom-route-exceptions-ip/05-CONTEXT.md
@.planning/phases/05-custom-route-exceptions-ip/05-RESEARCH.md

# Phase 4 vpn-status decisions (D-11..D-17 — flag parsing, filter ordering, journald query model)
@.planning/phases/04-traffic-logging-visibility-vpn-isp/04-CONTEXT.md

# Plan 01 of this phase (renamed paths feed into deploy.sh's final activation behavior)
@.planning/phases/05-custom-route-exceptions-ip/05-01-PLAN.md

# Current source-of-truth files this plan edits or creates
@scripts/vpn-status.sh
@deploy.sh
@.gitignore

<interfaces>
<!-- Existing patterns this plan extends. Verified against codebase. -->

scripts/vpn-status.sh (current state, before this plan):
  Lines 41-43: defaults block
    LAST=50
    FILTER=""
    DEVICE=""
  Lines 46-68: argument-parsing for-loop with case statement
    --last=* validates ^[0-9]+$
    --filter=* assigns FILTER="${arg#--filter=}"
    --device=* assigns DEVICE="${arg#--device=}"
    *) catch-all errors with usage hint and exits 1
  Lines 105-108: --device filter applied during entry collection (skip if src_ip != DEVICE)
  Lines 142-147: --filter applied after domain resolution (skip if FILTER not in domain, case-insensitive grep -iF)
  Lines 156-167: output block — prints header then iterates entries; each line is `ts|src_ip|dst_ip|domain|decision`

deploy.sh (current state, before this plan):
  Line 53-55: VPN_STATUS_LOCAL / VPN_STATUS_REMOTE / VPN_STATUS_TMP constants
  Line 60: TOTAL_STAGES=21
  Stage 18 (line 327): deploys configs/dnsmasq.conf
  Stage 19 (line 333): deploys scripts/vpn-status.sh
  Stage 20 (line 339): deploys scripts/watch-routes.py
  Stage 21 (line 345): `ssh ... "sudo ${ROUTING_SH_REMOTE} --no-update"` — final activation, currently uses --no-update

Stage template (verbatim, from Stage 19, lines 333-336):
  echo "[N/${TOTAL_STAGES}] Deploying X to ${SSH_HOST}:${X_REMOTE}..."
  scp -o BatchMode=yes "${X_LOCAL}" "${SSH_HOST}:${X_TMP}"
  ssh -o BatchMode=yes "${SSH_HOST}" "sudo mv ${X_TMP} ${X_REMOTE} && sudo chmod 644 ${X_REMOTE} && sudo chown root:root ${X_REMOTE}"
  echo "       X deployed (mode 644, root:root)."

.gitignore (current state — 11 lines):
  /.idea/
  /.claude/
  (blank)
  # Phase 1: VPN secrets — never commit
  .env.secrets
  /awg0.conf
  (blank)
  # Python bytecode cache
  __pycache__/
  *.pyc
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --via=vpn|isp flag to vpn-status.sh</name>
  <files>scripts/vpn-status.sh</files>
  <read_first>
    - scripts/vpn-status.sh (full file — 169 lines; see line ranges in <interfaces>)
    - .planning/phases/05-custom-route-exceptions-ip/05-CONTEXT.md (D-10, D-11)
    - .planning/phases/05-custom-route-exceptions-ip/05-RESEARCH.md (Pattern 2 — Flag Parsing Extension for --via; Pitfall 3 — apply filter at output time, not during entry collection)
    - .planning/phases/04-traffic-logging-visibility-vpn-isp/04-CONTEXT.md (D-14 filter ordering precedent)
  </read_first>
  <action>
    Add a VIA variable to the defaults block (after the DEVICE="" line at line 43): `VIA=""`. Add a new case arm to the existing argument-parsing for-loop, placed BEFORE the `*)` catch-all (so unknown arguments still error correctly): the new arm matches `--via=*)`, extracts `val="${arg#--via=}"`, validates `val` is either the literal string `vpn` or the literal string `isp` (and exits 1 with a clear error message if not), then assigns `VIA="${val^^}"` (uppercase — so `vpn`→`VPN` and `isp`→`ISP`, matching the `decision` field extracted from the iptables LOG prefix). Update the catch-all `err` usage hint string from `Usage: vpn-status.sh [--last=N] [--filter=STRING] [--device=IP]` to include `[--via=vpn|isp]`.

    Apply the --via filter at OUTPUT time (NOT during entry collection — Pitfall 3, performance). Insert the filter logic inside the output for-loop (the loop that begins `for entry in "${entries[@]}"` around line 163), immediately after the `IFS='|' read -r ts src_ip dst_ip domain decision <<< "${entry}"` line and BEFORE the `printf "%-20s ..."` line. The filter body MUST be: `if [[ -n "${VIA}" ]] && [[ "${decision}" != "${VIA}" ]]; then continue; fi`.

    Do NOT apply the --via filter during entry collection (do not place it next to the --device filter at lines 105-108). Pitfall 3 explicitly forbids this: putting it earlier triggers rDNS `host` lookups for entries that would be filtered out anyway, slowing the command. The --filter flag already uses the output-time placement; --via must match that precedent.

    Do NOT make --via mutually exclusive with --filter, --device, or --last (D-11). Do NOT change the fixed-string security model for --filter (already mitigated via grep -F — T-04-07). The --via value goes through a strict string-equality check, so no shell-injection surface is added.

    Concrete identifiers:
      - new variable name: VIA (alongside LAST, FILTER, DEVICE)
      - new case arm: `--via=*)` matching the existing case-arm style
      - valid values (case-sensitive in the argument): `vpn`, `isp`
      - stored VIA value: uppercase via `${val^^}` — `VPN` or `ISP`
      - filter condition (output loop): `[[ -n "${VIA}" ]] && [[ "${decision}" != "${VIA}" ]] && continue`
  </action>
  <verify>
    <automated>bash -n /Users/antonromankov/Projects/my-projects/split-tunneling-v2/scripts/vpn-status.sh && grep -qE '^VIA=""$' /Users/antonromankov/Projects/my-projects/split-tunneling-v2/scripts/vpn-status.sh && grep -q -- '--via=\*)' /Users/antonromankov/Projects/my-projects/split-tunneling-v2/scripts/vpn-status.sh && grep -q 'val\^\^' /Users/antonromankov/Projects/my-projects/split-tunneling-v2/scripts/vpn-status.sh && grep -q '\[--via=vpn|isp\]' /Users/antonromankov/Projects/my-projects/split-tunneling-v2/scripts/vpn-status.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n scripts/vpn-status.sh` exits 0
    - `scripts/vpn-status.sh` contains exactly one `VIA=""` initialization in the defaults block (between line ~43 and ~45)
    - `scripts/vpn-status.sh` contains a `--via=*)` case arm placed BEFORE the `*)` catch-all (verifiable: line number of `--via=*` < line number of the existing `*)` at line 62)
    - The `--via=*)` arm contains the literal substring `vpn` AND `isp` in its validation check
    - The `--via=*)` arm contains the literal substring `val^^` (uppercase parameter expansion)
    - The catch-all `*)` error message (`Usage: vpn-status.sh ...`) now contains the substring `[--via=vpn|isp]`
    - The output for-loop (`for entry in "${entries[@]}"`) contains the literal line/expression `[[ -n "${VIA}" ]]` AND `[[ "${decision}" != "${VIA}" ]]` AND `continue` — these MUST appear AFTER the `IFS='|' read -r ts src_ip dst_ip domain decision` line and BEFORE the `printf` line in the loop body (verifiable by line-number ordering)
    - The --via filter does NOT appear in the entry-collection while-loop (the block between lines 84-154 — verifiable: `[[ "${decision}" != "${VIA}" ]]` appears exactly once in the whole file, inside the output loop)
    - No new external command invocations (grep, awk, sed, etc.) introduced by the --via filter — pure shell string comparison
  </acceptance_criteria>
  <done>
    vpn-status.sh accepts --via=vpn and --via=isp, validates the value, uppercases it, applies the filter at output time, and rejects invalid --via values with a clear error.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add configs/white-list-extended.txt.example template and gitignore the real file</name>
  <files>configs/white-list-extended.txt.example, .gitignore</files>
  <read_first>
    - .gitignore (current 11 lines — see <interfaces>)
    - configs/dnsmasq.conf (existing file in configs/ — confirms the directory exists and the naming pattern)
    - .planning/phases/05-custom-route-exceptions-ip/05-CONTEXT.md (D-02, D-04)
    - .planning/phases/05-custom-route-exceptions-ip/05-RESEARCH.md (Open Question 1 — config/ vs configs/ resolution; verified repo uses configs/ for dnsmasq.conf)
  </read_first>
  <action>
    Resolution of Research Open Question #1: use `configs/` (plural) — the existing directory convention in this repo, already housing `configs/dnsmasq.conf`. CONTEXT.md D-04 says `config/` but Research Open Question #1 explicitly recommends `configs/` for consistency. Going with `configs/`.

    Create `configs/white-list-extended.txt.example` as a new committed file. Content MUST include:
      - A header comment block (lines beginning with `#`) explaining: the file's purpose (ISP-bypass exceptions on top of the auto-downloaded /etc/white-list.txt RU list), the format (one CIDR per line, identical to iplist output, no labels, no inline comments after a CIDR), how to use it (copy to `configs/white-list-extended.txt` — note: configs/ is gitignored for the real file — then run `./deploy.sh`), and a reminder that routing.sh trusts CIDR correctness and that malformed lines are silently dropped by `ip route add` (no validation).
      - 2-3 example CIDR entries (commented out with `#` at the start of the line so the example file does not itself route anything if accidentally deployed) showing realistic ISP-bypass targets. Pick: a small Steam content range and a small game-services range. Example:
          `# 23.55.0.0/16   # example: regional CDN (commented; remove # to activate)`
          `# 95.181.176.0/22 # example: a game-services range (commented; remove # to activate)`
        These are illustrative only — the format is "CIDR comment" on the same line is NOT supported by routing.sh's loop. The example file must use whole-line comments (`# 23.55.0.0/16`) NOT trailing comments (`23.55.0.0/16 # foo`). Re-state this constraint in the header.

    Add a new line to `.gitignore` ignoring the real (user-populated) exception file:
      - Insert immediately after the existing `/awg0.conf` line and before the blank-line that precedes the `# Python bytecode cache` block.
      - Two lines added: a section comment `# Phase 5: user-defined ISP-bypass exceptions — never commit` and the path `configs/white-list-extended.txt`.

    Do NOT gitignore `configs/white-list-extended.txt.example` — it is the committed template (D-04). Do NOT gitignore the entire `configs/` directory — `configs/dnsmasq.conf` and the .example file MUST remain tracked. The `.gitignore` entry MUST be exactly `configs/white-list-extended.txt` (no leading slash needed; git treats it as a project-relative path from the .gitignore directory).
  </action>
  <verify>
    <automated>test -f /Users/antonromankov/Projects/my-projects/split-tunneling-v2/configs/white-list-extended.txt.example && grep -q '^configs/white-list-extended.txt$' /Users/antonromankov/Projects/my-projects/split-tunneling-v2/.gitignore && [ $(wc -l < /Users/antonromankov/Projects/my-projects/split-tunneling-v2/configs/white-list-extended.txt.example) -ge 5 ] && grep -q '^#' /Users/antonromankov/Projects/my-projects/split-tunneling-v2/configs/white-list-extended.txt.example && cd /Users/antonromankov/Projects/my-projects/split-tunneling-v2 && git check-ignore -q configs/white-list-extended.txt && ! git check-ignore -q configs/white-list-extended.txt.example</automated>
  </verify>
  <acceptance_criteria>
    - File `configs/white-list-extended.txt.example` exists at the repo root under configs/
    - `configs/white-list-extended.txt.example` is at least 5 lines long
    - `configs/white-list-extended.txt.example` first line begins with `#` (header comment)
    - `configs/white-list-extended.txt.example` contains the substring `white-list-extended.txt` (referencing the deploy target) and `configs/white-list-extended.txt` (referencing the local user-populated file)
    - `configs/white-list-extended.txt.example` contains at least 2 example CIDR lines, ALL prefixed with `#` (commented out)
    - `.gitignore` contains the line `configs/white-list-extended.txt` (exact match, no trailing characters)
    - `git check-ignore -q configs/white-list-extended.txt` exits 0 (file path is ignored)
    - `git check-ignore -q configs/white-list-extended.txt.example` exits non-zero (template file is NOT ignored — it must be committable)
    - `configs/dnsmasq.conf` is NOT ignored (verifiable: `git check-ignore -q configs/dnsmasq.conf` exits non-zero)
  </acceptance_criteria>
  <done>
    configs/white-list-extended.txt.example is committed with a header explaining format and usage; configs/white-list-extended.txt is gitignored; configs/dnsmasq.conf and the .example template remain tracked.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add deploy.sh stage for conditional white-list-extended.txt deploy and drop --no-update from final activation</name>
  <files>deploy.sh</files>
  <read_first>
    - deploy.sh (full file — 437 lines; key line ranges in <interfaces>)
    - .planning/phases/05-custom-route-exceptions-ip/05-CONTEXT.md (D-12, D-13)
    - .planning/phases/05-custom-route-exceptions-ip/05-RESEARCH.md (Pattern 3 — Deploy Stage with Optional File; Pitfall 1 — Stage A preflight must NOT require this file; Pitfall 2 — first deploy after rename needs activation without --no-update; Open Question #2 — stage ordering)
    - .planning/phases/05-custom-route-exceptions-ip/05-01-PLAN.md (renamed paths produced by Plan 01 — routing.sh now writes to /etc/white-list.txt)
  </read_first>
  <action>
    Three coordinated edits to deploy.sh:

    (1) Add new constants in the configuration block (after the existing `WATCH_ROUTES_TMP=` line at line 58):
        WHITE_LIST_EXT_LOCAL="configs/white-list-extended.txt"
        WHITE_LIST_EXT_REMOTE="/etc/white-list-extended.txt"
        WHITE_LIST_EXT_TMP="/tmp/white-list-extended.tmp"

    (2) Bump TOTAL_STAGES at line 60 from `TOTAL_STAGES=21` to `TOTAL_STAGES=22`.

    (3) Insert a new conditional Stage 21 between the existing Stage 20 (watch-routes.py deploy, around line 339-342) and the existing final routing.sh activation stage (currently Stage 21, around line 345-347). After insertion, the existing final activation becomes Stage 22. The new Stage 21 MUST:
        - Print `echo "[21/${TOTAL_STAGES}] Deploying white-list-extended.txt to ${SSH_HOST} (if present)..."`.
        - Check local file presence: `if [[ -f "${WHITE_LIST_EXT_LOCAL}" ]]; then ... else ... fi`.
        - On present: `scp -o BatchMode=yes "${WHITE_LIST_EXT_LOCAL}" "${SSH_HOST}:${WHITE_LIST_EXT_TMP}"` then `ssh -o BatchMode=yes "${SSH_HOST}" "sudo mv ${WHITE_LIST_EXT_TMP} ${WHITE_LIST_EXT_REMOTE} && sudo chmod 644 ${WHITE_LIST_EXT_REMOTE} && sudo chown root:root ${WHITE_LIST_EXT_REMOTE}"` then `echo "       white-list-extended.txt deployed (mode 644, root:root)."`. Use chmod 644 NOT chmod +x — this is a data file, not an executable (matches dnsmasq.conf precedent at line 329).
        - On absent: `echo "       ${WHITE_LIST_EXT_LOCAL} not found in repo — skipping exception file deploy (D-05)."` and continue (no error).

    (4) Renumber the existing final activation stage label from `[21/${TOTAL_STAGES}]` to `[22/${TOTAL_STAGES}]`. Modify its `ssh -o BatchMode=yes ... "sudo ${ROUTING_SH_REMOTE} --no-update"` command by REMOVING `--no-update` so the line becomes `ssh -o BatchMode=yes "${SSH_HOST}" "sudo ${ROUTING_SH_REMOTE}"`. This addresses Research Pitfall 2: after Plan 01's rename, /etc/white-list.txt does not yet exist on the RPi; --no-update would abort routing.sh. Dropping --no-update forces a fresh download to the new path on first deploy. The downstream tradeoff (cron's `update-vpn-routes` script also calls --no-update — but only after a separate fresh download in /tmp; that flow is unaffected because update-vpn-routes does its own download first).

    (5) Update the final-summary echo block at the bottom of deploy.sh (the long block starting around line 353 with `Phase 1 + 2 + 3 ...`). Add a new bullet under " Deployed:" that reads `echo "   PHASE 5: ${WHITE_LIST_EXT_REMOTE} (mode 644, root:root, optional — deployed only if ${WHITE_LIST_EXT_LOCAL} exists)"`. Add a Phase 5 verification block (after the existing " Phase 4 verification:" block, before the closing `================================================================`) with these echo lines:
        echo " Phase 5 verification:"
        echo "   ssh pi4 \"sudo /etc/vpn-status.sh --via=vpn\""
        echo "   # expect: only rows with VPN in the PATH column (or empty if no VPN traffic)"
        echo "   ssh pi4 \"sudo /etc/vpn-status.sh --via=isp\""
        echo "   # expect: only rows with ISP in the PATH column"
        echo "   ssh pi4 \"ls -l /etc/white-list-extended.txt 2>/dev/null || echo 'no exception file present'\""
        echo "   ssh pi4 \"sudo /etc/routing.sh && ip route get <YOUR-EXCEPTION-CIDR-IP>\""
        echo "   # expect: route via 192.168.1.1 (KEENETIC_GW) for any IP inside an exception CIDR"

    (6) Do NOT add `WHITE_LIST_EXT_LOCAL` to Stage A preflight required-files block (lines 89-138). Pitfall 1 explicitly forbids this — the file's absence is a normal state per D-05. Only the .example file MIGHT be referenced, but it is not required either (the deploy works without it; the .example exists for human guidance, not deploy-time consumption).

    Concrete identifier table:
      - new local variable: WHITE_LIST_EXT_LOCAL = "configs/white-list-extended.txt"
      - new remote variable: WHITE_LIST_EXT_REMOTE = "/etc/white-list-extended.txt"
      - new tmp variable: WHITE_LIST_EXT_TMP = "/tmp/white-list-extended.tmp"
      - TOTAL_STAGES: 21 → 22
      - new stage number: 21 (between watch-routes.py and routing.sh activation)
      - existing activation: renumbered from 21 → 22 AND --no-update removed
  </action>
  <verify>
    <automated>bash -n /Users/antonromankov/Projects/my-projects/split-tunneling-v2/deploy.sh && grep -q '^WHITE_LIST_EXT_LOCAL="configs/white-list-extended.txt"$' /Users/antonromankov/Projects/my-projects/split-tunneling-v2/deploy.sh && grep -q '^TOTAL_STAGES=22$' /Users/antonromankov/Projects/my-projects/split-tunneling-v2/deploy.sh && ! grep -q 'TOTAL_STAGES=21' /Users/antonromankov/Projects/my-projects/split-tunneling-v2/deploy.sh && grep -q '\[21/\${TOTAL_STAGES}\] Deploying white-list-extended.txt' /Users/antonromankov/Projects/my-projects/split-tunneling-v2/deploy.sh && grep -q '\[22/\${TOTAL_STAGES}\]' /Users/antonromankov/Projects/my-projects/split-tunneling-v2/deploy.sh && ! grep -E 'sudo \$\{ROUTING_SH_REMOTE\} --no-update' /Users/antonromankov/Projects/my-projects/split-tunneling-v2/deploy.sh && grep -q 'Phase 5 verification' /Users/antonromankov/Projects/my-projects/split-tunneling-v2/deploy.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n deploy.sh` exits 0
    - `deploy.sh` contains exactly one `^TOTAL_STAGES=22$` line and zero `^TOTAL_STAGES=21$` lines
    - `deploy.sh` contains all three new constant lines: `WHITE_LIST_EXT_LOCAL="configs/white-list-extended.txt"`, `WHITE_LIST_EXT_REMOTE="/etc/white-list-extended.txt"`, `WHITE_LIST_EXT_TMP="/tmp/white-list-extended.tmp"`
    - `deploy.sh` has a stage labeled `[21/${TOTAL_STAGES}]` whose body contains `if [[ -f "${WHITE_LIST_EXT_LOCAL}" ]]` AND `scp -o BatchMode=yes "${WHITE_LIST_EXT_LOCAL}"` AND `sudo chmod 644 ${WHITE_LIST_EXT_REMOTE}` AND a `not found in repo — skipping` log message on the absent branch
    - `deploy.sh` has a stage labeled `[22/${TOTAL_STAGES}]` whose body contains `ssh -o BatchMode=yes "${SSH_HOST}" "sudo ${ROUTING_SH_REMOTE}"` (NOT `sudo ${ROUTING_SH_REMOTE} --no-update`)
    - `grep -c -- '--no-update' deploy.sh` returns 0 (after this edit, deploy.sh no longer passes --no-update; manual user-facing instructions in the final summary may still reference it but should not — verify any remaining occurrence is in a help/comment context, not an ssh command line)
    - Stage A preflight (lines ~89-138) does NOT contain `WHITE_LIST_EXT_LOCAL` — confirms Pitfall 1 avoided
    - Final summary block contains the literal string `Phase 5 verification:` and references `--via=vpn` AND `--via=isp` in its example commands
    - Final summary " Deployed:" section contains a line referencing `${WHITE_LIST_EXT_REMOTE}` AND the substring `optional`
    - File length increased by approximately 20-30 lines vs pre-edit (sanity check; no exact equality required)
  </acceptance_criteria>
  <done>
    deploy.sh deploys configs/white-list-extended.txt to /etc/white-list-extended.txt on the RPi when the local file exists (mode 644, root:root) and logs a skip line otherwise; the new stage is wired as Stage 21 with the existing final activation renumbered to Stage 22; the final activation no longer uses --no-update so the first Phase 5 deploy correctly downloads to the renamed /etc/white-list.txt path; Stage A preflight remains free of any required-file check for the user's exception file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| macOS workstation → RPi via SSH | deploy.sh copies a user-controlled file (configs/white-list-extended.txt) into a root-owned location on the RPi |
| User input → vpn-status.sh CLI flags | --via value is parsed from command-line, must not introduce shell injection |
| Local file presence → deploy.sh branch selection | Optional file controls whether the SCP path executes; absence must be a non-error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | --via flag value injected into shell or grep | mitigate | --via value validated against the exact strings `vpn` or `isp` before any use; rejected values exit 1. Only the uppercased VIA variable is used downstream, and only inside a `[[ ... != ... ]]` bash conditional — never passed to `eval`, `sh -c`, or grep pattern. Matches T-04-07 mitigation model used for --filter. |
| T-05-07 | Tampering | configs/white-list-extended.txt content during SCP | mitigate | SCP runs under the developer's account; remote `sudo mv` + `sudo chmod 644` + `sudo chown root:root` lock the file to root before routing.sh reads it. Same chain-of-trust as dnsmasq.conf and routing.sh deploy. Path is hardcoded — no variable interpolation from file content. |
| T-05-08 | Denial of Service | Stage 21 fails if SCP fails mid-transfer, leaving stale /tmp/white-list-extended.tmp on RPi | accept | /tmp is cleared on RPi reboot; orphan tmp files in /tmp are harmless. Matches existing pattern for all other deploy stages (no rollback-on-mid-SCP-failure logic anywhere in deploy.sh). |
| T-05-09 | Tampering | Removing --no-update from Stage 22 means every deploy re-downloads the RU CIDR list, increasing network calls | accept | curl `-fsSL` retries are absent (consistent with Phase 2 D-04), so a failed download falls back to the existing file. Daily cron via update-vpn-routes already downloads independently. Net effect: one extra download per deploy run (operator-initiated), acceptable cost. |
| T-05-10 | Information Disclosure | configs/white-list-extended.txt is gitignored but could be committed by accident if .gitignore is bypassed | accept | File contents are CIDR ranges (publicly-known IP allocations), not secrets. Threat model matches Phase 1 .env.secrets handling principle (separation of concerns) but with lower stakes (no cryptographic material). |
| T-{phase}-SC | Tampering | npm/pip/cargo installs | n/a | Phase 5 installs no packages — confirmed by 05-RESEARCH Standard Stack and Package Legitimacy Audit sections. |
</threat_model>

<verification>
On a clean repo checkout (no live RPi required for static checks):

1. `bash -n scripts/vpn-status.sh` exits 0
2. `bash -n deploy.sh` exits 0
3. `git check-ignore -q configs/white-list-extended.txt` exits 0
4. `git check-ignore -q configs/white-list-extended.txt.example` exits non-zero
5. `git check-ignore -q configs/dnsmasq.conf` exits non-zero
6. `grep -c -- '--no-update' deploy.sh` returns 0 inside any `ssh ...` command line (residual references only in usage hints / final-summary helper text, if any)
7. `grep -E '^TOTAL_STAGES=' deploy.sh` returns exactly one match, equal to `TOTAL_STAGES=22`
8. `bash -c 'source /dev/null; declare -A; case --via=vpn in --via=*) val="${1#--via=}"; echo "${val^^}"; esac' --via=vpn` confirms the bash `${val^^}` uppercase expansion semantics work on the target bash version (already used elsewhere in the codebase; included here as a sanity check)

Live-RPi smoke (deferred to executor's choice; not required to mark plan complete — these are Phase 5 acceptance, not Plan 02 acceptance):
  - `ssh pi4 "sudo /etc/vpn-status.sh --via=vpn"` lists only VPN rows
  - `ssh pi4 "sudo /etc/vpn-status.sh --via=isp"` lists only ISP rows
  - `ssh pi4 "sudo /etc/vpn-status.sh --via=foo"` exits 1 with the validation error
  - After `./deploy.sh` with `configs/white-list-extended.txt` containing one CIDR (e.g. `203.0.113.0/24`), `ssh pi4 "ip route get 203.0.113.1"` shows `via 192.168.1.1`
  - After `./deploy.sh` with `configs/white-list-extended.txt` absent, the new Stage 21 prints `not found in repo — skipping` and routing.sh logs `Stage 5b: ... not found — no exception routes loaded (D-05)`
</verification>

<success_criteria>
- vpn-status.sh accepts --via=vpn and --via=isp; rejects all other --via values
- vpn-status.sh --via composes with --filter, --device, --last
- configs/white-list-extended.txt.example committed; configs/white-list-extended.txt gitignored
- deploy.sh has a new stage (Stage 21) that conditionally deploys the exception file with chmod 644 root:root
- deploy.sh TOTAL_STAGES is 22; final activation stage no longer passes --no-update
- Stage A preflight does NOT require configs/white-list-extended.txt (Pitfall 1)
- Final-summary block documents the Phase 5 verification commands including --via=vpn and --via=isp
- All scripts pass `bash -n` syntax check
</success_criteria>

<output>
Create `.planning/phases/05-custom-route-exceptions-ip/05-02-SUMMARY.md` when done. Record: the new line numbers of the inserted Stage 21 block, the final TOTAL_STAGES value, the exact .gitignore entry committed, and a copy of the configs/white-list-extended.txt.example header to confirm the format documented to the user.
</output>
