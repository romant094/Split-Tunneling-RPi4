---
phase: 16-web-admin-ux-upgrade
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/admin/src/components/ui/progress.jsx
  - src/admin/src/pages/Dashboard.jsx
autonomous: true
requirements: [UI-RESOURCES]
must_haves:
  truths:
    - "The Dashboard Resources card renders a progress bar for CPU%, RAM (used/total), and Disk (used/total), not just numbers"
    - "Each bar's fill width reflects the current percentage from the SSE /api/resources/watch data"
    - "The existing 5s SSE cadence is retained (no change to refresh interval)"
  artifacts:
    - path: "src/admin/src/components/ui/progress.jsx"
      provides: "reusable Progress bar component"
      min_lines: 8
    - path: "src/admin/src/pages/Dashboard.jsx"
      provides: "progress bars in Resources card"
      contains: "Progress"
  key_links:
    - from: "src/admin/src/pages/Dashboard.jsx Resources card"
      to: "resources.cpu_percent / mem_used / disk_used"
      via: "Progress value prop = computed percent"
      pattern: "Progress"
---

<objective>
Give the Dashboard Resources card visual progress bars for CPU, RAM, and Disk (UI-RESOURCES) instead of bare numbers, using the data already streamed over SSE. Keep the existing 5s cadence — the CONTEXT deferred idea (1s vs 5s) resolves to "keep current cadence" per RESEARCH (negligible-benefit perf tradeoff).

Purpose: Numbers alone don't convey utilization at a glance; bars do. Data is already available client-side.
Output: New `progress.jsx` UI primitive + updated `Dashboard.jsx`; committed `dist/` rebuild.
</objective>

<execution_context>
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-web-admin-ux-upgrade/16-CONTEXT.md
@.planning/phases/16-web-admin-ux-upgrade/16-RESEARCH.md

<interfaces>
Dashboard.jsx (read this session):
- SSE data shape from `/api/resources/watch`: `{ cpu_percent (float 0-100), mem_used, mem_total, disk_used, disk_total (bytes), services:[{name,cpu,mem}] }`
- `useSSE('/api/resources/watch', setResources)` at line 57; `resources` state
- Resources card at lines 140-190: CPU row shows `${resources.cpu_percent}%`; RAM row shows `${fmtGB(mem_used)} / ${fmtGB(mem_total)}`; Disk row shows `${fmtGB(disk_used)} / ${fmtGB(disk_total)}`. `fmtGB`/`fmtMem` helpers already exist in the file.
- Existing UI components live in `src/admin/src/components/ui/*.jsx` (button, badge, card, etc.). No `progress.jsx` exists yet.
- Styling: Tailwind v4 with CSS-var theme (`bg-primary`, `bg-muted`, `text-muted-foreground`); `cn` helper at `@/lib/utils`.

CADENCE DECISION: do NOT change the SSE interval; leave `/api/resources/watch` at its current 5s (backend `time.sleep(5)`). This task is render-only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Progress UI component</name>
  <files>src/admin/src/components/ui/progress.jsx</files>
  <read_first>
    - src/admin/src/components/ui/badge.jsx (as a style/idiom reference for a small presentational component)
    - src/admin/src/lib/utils.js (confirm the `cn` export path used elsewhere)
  </read_first>
  <action>
    Create a self-contained `Progress` component (no Radix dependency — keep it Tailwind-only to avoid a new npm package per RESEARCH audit). It accepts a `value` prop (0-100 number) and optional `className`. Render an outer track div (`bg-muted` or `bg-secondary`, classes `h-2 rounded-full overflow-hidden w-full`) containing an inner fill div with classes `bg-primary h-full rounded-full transition-all` and an inline style setting `width` to a clamped percentage computed via `Math.min(100, Math.max(0, value))` followed by `%`. Export it as a named export `Progress`. Use the `cn` helper for className merging to match the other ui components.
  </action>
  <verify>
    <automated>cd src/admin && node -e "const s=require('fs').readFileSync('src/components/ui/progress.jsx','utf8'); if(!/export.*Progress/.test(s)) throw new Error('no Progress export'); if(!s.includes('width')) throw new Error('no width fill'); if(!s.includes('Math.min')) throw new Error('no clamp'); console.log('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - file `src/admin/src/components/ui/progress.jsx` exists and exports `Progress`
    - it computes a clamped width percentage from a `value` prop (grep for `Math.min` and `width`)
    - `grep -c radix src/admin/src/components/ui/progress.jsx` returns 0 (no new external progress package)
  </acceptance_criteria>
  <done>A reusable clamped Progress bar exists with zero new dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Render CPU/RAM/Disk progress bars in the Resources card</name>
  <files>src/admin/src/pages/Dashboard.jsx</files>
  <read_first>
    - src/admin/src/pages/Dashboard.jsx (read the Resources card block lines 140-190 in full, and the `fmtGB`/`fmtMem` helper definitions near the top)
    - src/admin/src/components/ui/progress.jsx (the component created in Task 1)
  </read_first>
  <action>
    Import `{ Progress }` from `@/components/ui/progress`. In the Resources card's three system rows, keep the existing numeric labels but add a `Progress` bar for each metric:
    - CPU bar: `value={resources?.cpu_percent ?? 0}`.
    - RAM bar: `value={resources && resources.mem_total ? (resources.mem_used / resources.mem_total) * 100 : 0}` (guard divide-by-zero when `mem_total` is 0/undefined).
    - Disk bar: `value={resources && resources.disk_total ? (resources.disk_used / resources.disk_total) * 100 : 0}` (same guard).
    Keep the existing `fmtGB(...)` used/total and `cpu_percent%` text next to each bar. Restructure each of the three rows into a small vertical stack (label + numeric readout on one line, `Progress` bar below) so the bar spans the card width. Do NOT alter the per-service table below, the SSE wiring, `useSSE`, or the refresh cadence.
  </action>
  <verify>
    <automated>cd src/admin && node -e "const s=require('fs').readFileSync('src/pages/Dashboard.jsx','utf8'); if(!s.includes('Progress')) throw new Error('no Progress usage'); if(!s.includes('mem_used')||!s.includes('disk_used')) throw new Error('missing resource fields'); console.log('OK')" && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Progress" src/admin/src/pages/Dashboard.jsx` returns >= 3 (import + 3 usages, so >= 4 acceptable)
    - RAM and Disk bars guard against divide-by-zero (grep for `mem_total ?` and `disk_total ?`)
    - `cd src/admin && npx vite build` completes exit 0 and regenerates `dist/`
    - the SSE interval/backend cadence is unchanged (no edit to splitgate-admin.py in this plan; `resources/watch` not touched)
  </acceptance_criteria>
  <done>Resources card shows clamped CPU/RAM/Disk bars fed by live SSE data; cadence unchanged; build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → Browser | Resource metrics rendered client-side; no user input crosses back |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-09 | Information Disclosure | Resources card | accept | Metrics already exposed via existing authenticated SSE endpoint (Phase 15); this plan only changes rendering |
| T-16-SC | Tampering | npm build | mitigate | No new npm packages (Progress is hand-rolled Tailwind); RESEARCH audit records zero additions |
</threat_model>

<verification>
- progress.jsx exists with clamped Progress export
- Dashboard.jsx renders 3 bars with divide-by-zero guards; `vite build` succeeds
</verification>

<success_criteria>
CPU/RAM/Disk each render a live progress bar; interval unchanged; zero new dependencies.
</success_criteria>

<output>
Create `.planning/phases/16-web-admin-ux-upgrade/16-04-SUMMARY.md` when done
</output>
