---
phase: 16-web-admin-ux-upgrade
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/deploy.sh
  - README.md
  - docs/README.ru.md
  - docs/REFERENCE.md
autonomous: true
requirements: [UI-DEPLOY, UI-DIAG]
must_haves:
  truths:
    - "Running deploy.sh twice does not overwrite an already-present /etc/splitgate/vpn-gateway.env on the RPi"
    - "Running deploy.sh twice does not overwrite an already-present /etc/amnezia/amneziawg/awg0.conf on the RPi"
    - "A first-time deploy (remote file absent) still deploys both config files"
    - "deploy.sh installs the traceroute apt package on the RPi when absent, before Diagnostics can use it"
  artifacts:
    - path: "src/deploy.sh"
      provides: "guarded (skip-if-exists) env + awg0.conf deploy; traceroute install stage"
      contains: "traceroute"
  key_links:
    - from: "src/deploy.sh Stage G/H"
      to: "remote existence check via ssh test -f"
      via: "conditional skip-if-present with warning"
      pattern: "test -f"
---

<objective>
Make `deploy.sh` non-destructive on redeploy (UI-DEPLOY) so RPi-side edits made through the Settings page survive a re-run, and add a `traceroute` apt-install stage (UI-DIAG dependency) mirroring the existing dnsmasq install-then-configure pattern.

Purpose: The Settings page (Phase 15) writes to the same `vpn-gateway.env` and `awg0.conf` that deploy.sh unconditionally overwrites — every redeploy silently clobbers those edits (RESEARCH Pitfalls 2 & 3). Diagnostics traceroute needs the binary present.
Output: Updated `src/deploy.sh`; docs synced.
</objective>

<execution_context>
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-web-admin-ux-upgrade/16-CONTEXT.md
@.planning/phases/16-web-admin-ux-upgrade/16-RESEARCH.md

<interfaces>
Existing deploy.sh structure (read this session):
- `TOTAL_STAGES=29` (line 101)
- `AWG_CONF_REMOTE="/etc/amnezia/amneziawg/awg0.conf"` (line 40), `ENV_REMOTE="/etc/splitgate/vpn-gateway.env"` (line 41)
- Stage G `[7/${TOTAL_STAGES}]` (line 288) — renders + SCPs awg0.conf, unconditional `sudo mv` overwrite
- Stage H `[8/${TOTAL_STAGES}]` (line 302) — `cat ../.env > env_merged_tmp`, unconditional `sudo mv` overwrite of ENV_REMOTE
- Stage 18 dnsmasq install PRECEDENT `[17/${TOTAL_STAGES}]` (line 393): `ssh -o BatchMode=yes "${SSH_HOST}" "if ! dpkg -l dnsmasq 2>/dev/null | grep -q '^ii'; then sudo DEBIAN_FRONTEND=noninteractive apt-get install -y dnsmasq; fi"`
- RESEARCH-provided traceroute install one-liner: `ssh -o BatchMode=yes "${SSH_HOST}" "if ! dpkg -l traceroute 2>/dev/null | grep -q '^ii'; then sudo DEBIAN_FRONTEND=noninteractive apt-get install -y traceroute; fi"`

Chosen non-destructive strategy (RESEARCH Open Question 2, planner decision): **skip-if-exists with a warning**. On redeploy, if the remote file already exists, skip the overwrite and print a warning telling the operator to edit via the Settings page or delete the remote file to force a re-deploy. First deploy (remote absent) proceeds normally. This is the simplest correct strategy and matches the Settings-page-is-source-of-truth reality for these two files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: UI-DEPLOY — guard awg0.conf and vpn-gateway.env overwrites (skip-if-exists)</name>
  <files>src/deploy.sh</files>
  <read_first>
    - src/deploy.sh (read Stage G lines 287-299 and Stage H lines 301-313 in full, plus Stage I verification 315-347 to keep the existence assertions consistent)
    - .planning/phases/16-web-admin-ux-upgrade/16-RESEARCH.md (Pitfall 2 and Pitfall 3)
  </read_first>
  <action>
    In Stage G (awg0.conf deploy, `[7/${TOTAL_STAGES}]`): before the `scp "$tmp" ...` + `sudo mv ... ${AWG_CONF_REMOTE}` block, add a guard that runs `ssh "$SSH_HOST" "sudo test -f ${AWG_CONF_REMOTE}"`. If it returns 0 (file exists), echo a warning line `       awg0.conf already present on ${SSH_HOST} — skipping overwrite (edit via Settings page, or delete the remote file to force redeploy)` and skip the scp/mv/chmod/chown steps for this file (still keep the local render into `$tmp` if `$tmp` is reused elsewhere, but do not push it). If the remote file is absent, run the existing deploy steps unchanged.
    In Stage H (vpn-gateway.env deploy, `[8/${TOTAL_STAGES}]`): apply the same guard against `${ENV_REMOTE}` — `ssh "$SSH_HOST" "test -f ${ENV_REMOTE}"` (env file is 644 root:root so no sudo needed for test, but use sudo test to match Stage I). If present, echo `       vpn-gateway.env already present — skipping overwrite (edit via Settings page)` and skip. If absent, run the existing `cat ../.env > "$env_merged_tmp"` → scp → mv path unchanged.
    Keep Stage I's post-deploy verification (`test -f ${ENV_REMOTE}` / `test -f ${AWG_CONF_REMOTE}`) intact — it must still assert both files exist (they do, whether freshly deployed or pre-existing).
    Do NOT change `TOTAL_STAGES` for this task (no stage added/removed).
  </action>
  <verify>
    <automated>bash -n src/deploy.sh && grep -c "already present" src/deploy.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n src/deploy.sh` exits 0 (syntax valid)
    - `grep -c "sudo test -f ${AWG_CONF_REMOTE}\|test -f ${AWG_CONF_REMOTE}" src/deploy.sh` returns >= 2 (one in Stage G guard, one in Stage I verify)
    - both Stage G and Stage H print a "skipping overwrite" style warning when the remote file exists (grep `already present` returns >= 2)
    - Stage I verification block still contains `test -f ${ENV_REMOTE}` and `test -f ${AWG_CONF_REMOTE}`
  </acceptance_criteria>
  <done>Redeploy skips overwriting an existing env/awg0.conf with a clear warning; first deploy still writes both.</done>
</task>

<task type="auto">
  <name>Task 2: UI-DIAG — add traceroute apt-install stage</name>
  <files>src/deploy.sh</files>
  <read_first>
    - src/deploy.sh (read the dnsmasq install Stage 18 at lines 390-395 as the exact precedent to mirror, and the `TOTAL_STAGES=29` line 101)
    - .planning/phases/16-web-admin-ux-upgrade/16-RESEARCH.md (Standard Stack installation one-liner, Environment Availability)
  </read_first>
  <action>
    Add a new stage that installs `traceroute` on the RPi if not already present, mirroring the dnsmasq Stage 18 idempotent pattern. Place it near the dnsmasq stage (after Stage 19 dnsmasq config deploy is fine, or immediately after Stage 18) so it groups with other apt installs. Increment `TOTAL_STAGES` from 29 to 30, and renumber the stage-progress labels (`[N/${TOTAL_STAGES}]`) of all stages at/after the insertion point by +1 so the counter stays monotonic and ends at 30. The install command is: `ssh -o BatchMode=yes "${SSH_HOST}" "if ! dpkg -l traceroute 2>/dev/null | grep -q '^ii'; then sudo DEBIAN_FRONTEND=noninteractive apt-get install -y traceroute; fi"` followed by an echo `       traceroute installed (or already present)`.
    Grep the file after editing to confirm no two stages share the same `[N/30]` label and the final stage is `[30/30]`.
  </action>
  <verify>
    <automated>bash -n src/deploy.sh && grep -c "apt-get install -y traceroute" src/deploy.sh && grep -c "TOTAL_STAGES=30" src/deploy.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n src/deploy.sh` exits 0
    - `grep -c "apt-get install -y traceroute" src/deploy.sh` returns 1
    - `grep -c "TOTAL_STAGES=30" src/deploy.sh` returns 1
    - `grep -oE '\[[0-9]+/\$\{TOTAL_STAGES\}\]' src/deploy.sh | sort -t'[' -k2 -n | uniq -d` returns empty (no duplicate stage numbers)
    - a `[30/${TOTAL_STAGES}]` label exists (final stage renumbered)
  </acceptance_criteria>
  <done>deploy.sh idempotently installs traceroute; stage counter is contiguous 1..30.</done>
</task>

<task type="auto">
  <name>Task 3: Sync docs for deploy behavior change + traceroute dependency</name>
  <files>README.md, docs/README.ru.md, docs/REFERENCE.md</files>
  <read_first>
    - README.md (grep for the deploy section and any mention of vpn-gateway.env / awg0.conf overwrite behavior)
    - docs/REFERENCE.md (grep for the deploy stage table / apt package list)
    - docs/README.ru.md (locate the matching Russian sections to keep in sync per CLAUDE.md convention)
  </read_first>
  <action>
    Document the two behavior changes in all three docs (English README.md + docs/REFERENCE.md, Russian docs/README.ru.md — English-only content, Russian translation in the .ru file per project convention): (1) `deploy.sh` no longer overwrites an existing `/etc/splitgate/vpn-gateway.env` or `/etc/amnezia/amneziawg/awg0.conf` on redeploy — to change these post-first-deploy, edit via the Settings page or delete the remote file to force a fresh deploy; (2) `deploy.sh` now installs the `traceroute` apt package (required by the Diagnostics page). If docs/REFERENCE.md has a deploy-stages table, add/renumber the traceroute stage and bump the total to 30. Only touch the sections that describe deploy behavior or the apt dependency list; do not rewrite unrelated content.
  </action>
  <verify>
    <automated>grep -l "traceroute" README.md docs/REFERENCE.md docs/README.ru.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "traceroute" README.md` >= 1 AND `grep -c "traceroute" docs/README.ru.md` >= 1 AND `grep -c "traceroute" docs/REFERENCE.md` >= 1
    - each doc mentions that redeploy no longer overwrites existing env/awg0.conf (grep for `Settings page` or `overwrite` near the deploy section returns >= 1 in README.md and docs/README.ru.md)
  </acceptance_criteria>
  <done>All three docs describe the non-destructive redeploy behavior and the traceroute dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev macOS → RPi (SSH) | deploy.sh pushes config + runs apt as root over SSH |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Tampering (data loss) | deploy.sh overwrite of live config | mitigate | Skip-if-exists guard prevents clobbering RPi-side edits made via Settings page (the actual UI-DEPLOY bug) |
| T-16-06 | Elevation | apt-get install over SSH | accept | Runs on the operator's own RPi over key-only SSH (D-05/D-06); traceroute is a mainstream Debian package |
| T-16-SC | Tampering | apt install (traceroute) | mitigate | traceroute is an OS-level Debian bookworm package, not a language-ecosystem package; RESEARCH Package Legitimacy Audit records zero npm/pip additions. Idempotent `dpkg -l` guard before install |
</threat_model>

<verification>
- `bash -n src/deploy.sh` passes
- TOTAL_STAGES=30, contiguous stage numbering, traceroute install present, both config guards present
- docs mention traceroute + non-destructive redeploy
</verification>

<success_criteria>
Redeploy preserves existing env/awg0.conf; traceroute is installed idempotently; docs synced.
</success_criteria>

<output>
Create `.planning/phases/16-web-admin-ux-upgrade/16-02-SUMMARY.md` when done
</output>
