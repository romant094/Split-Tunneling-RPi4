---
phase: "15-web-admin-interface"
plan: 04
type: execute
wave: 3
depends_on:
  - "15-02"
  - "15-03"
files_modified:
  - src/scripts/splitgate
  - README.md
  - docs/REFERENCE.md
  - docs/README.ru.md
  - .planning/STATE.md
autonomous: true
requirements:
  - WEB-05
  - WEB-06

must_haves:
  truths:
    - "splitgate admin status shows whether splitgate-admin.service is active or inactive"
    - "splitgate admin start/stop/restart controls splitgate-admin.service via systemctl"
    - "splitgate admin /? prints usage with status/start/stop/restart subcommands"
    - "splitgate with no args or unknown subcommand shows updated Usage line including admin"
    - "README.md has a Web Admin Interface section (concise — consistent with ~150 line target from Phase 11)"
    - "docs/REFERENCE.md has Phase 15 API reference section with full endpoint table"
    - "docs/README.ru.md is updated to match README.md Web Admin section in Russian"
    - ".planning/STATE.md records Phase 15 decisions D-01 through D-14"
  artifacts:
    - path: "src/scripts/splitgate"
      provides: "Updated dispatcher with admin subcommand (status/start/stop/restart)"
      min_lines: 130
    - path: "README.md"
      provides: "Web Admin Interface section"
      min_lines: 150
    - path: "docs/REFERENCE.md"
      provides: "Phase 15 API endpoint table"
      min_lines: 200
  key_links:
    - from: "splitgate admin status"
      to: "systemctl is-active splitgate-admin.service"
      via: "exec sudo systemctl is-active splitgate-admin.service"
    - from: "splitgate admin start|stop|restart"
      to: "systemctl {action} splitgate-admin.service"
      via: "exec sudo systemctl ${action} splitgate-admin.service"
---

<objective>
Add the `admin` subcommand to the splitgate dispatcher and update all documentation to cover Phase 15.

Purpose: Make the web admin interface manageable via the existing CLI (`splitgate admin status/start/stop/restart`). Document the admin interface, all API endpoints, and Phase 15 decisions for future operators.

Output: Updated src/scripts/splitgate (admin subcommand); updated README.md, docs/REFERENCE.md, docs/README.ru.md with web admin section; updated .planning/STATE.md with Phase 15 decisions.
</objective>

<execution_context>
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/workflows/execute-plan.md
@/Users/antonromankov/Projects/my-projects/split-tunneling-v2/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-web-admin-interface/15-CONTEXT.md
@src/scripts/splitgate
@README.md
@docs/REFERENCE.md
@docs/README.ru.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add admin subcommand to src/scripts/splitgate dispatcher</name>
  <files>src/scripts/splitgate</files>
  <action>
Edit src/scripts/splitgate to add the admin subcommand. Read the existing file first; preserve all existing subcommands (status, watch, rollback, routing, update) unchanged.

CHANGES NEEDED:

1. Update the Usage line in the `*)` catch-all case:
   Change: echo "Usage: splitgate {status|watch|rollback|routing|update} [args...]"
   To:     echo "Usage: splitgate {status|watch|rollback|routing|update|admin} [args...]"

2. Add a new `admin)` case BEFORE the `*)` catch-all (after the `update)` case block):

  admin)
    ADMIN_ACTION="${1:-}"
    shift || true
    case "$ADMIN_ACTION" in
      status)
        if $has_help; then
          echo "splitgate admin status — show splitgate-admin.service state"
          echo ""
          echo "Usage: splitgate admin status"
          echo ""
          echo "  Reports whether the web admin interface service is active."
          echo "  Access the admin interface at http://192.168.1.254:8080"
          exit 0
        fi
        exec sudo systemctl is-active splitgate-admin.service
        ;;
      start)
        if $has_help; then
          echo "splitgate admin start — start the splitgate web admin service"
          echo ""
          echo "Usage: splitgate admin start"
          exit 0
        fi
        exec sudo systemctl start splitgate-admin.service
        ;;
      stop)
        if $has_help; then
          echo "splitgate admin stop — stop the splitgate web admin service"
          echo ""
          echo "Usage: splitgate admin stop"
          exit 0
        fi
        exec sudo systemctl stop splitgate-admin.service
        ;;
      restart)
        if $has_help; then
          echo "splitgate admin restart — restart the splitgate web admin service"
          echo ""
          echo "Usage: splitgate admin restart"
          exit 0
        fi
        exec sudo systemctl restart splitgate-admin.service
        ;;
      *)
        echo "Usage: splitgate admin {status|start|stop|restart}"
        echo "       splitgate admin <subcommand> /? — per-subcommand help"
        echo ""
        echo "  status   — show service state"
        echo "  start    — start the admin web interface"
        echo "  stop     — stop the admin web interface"
        echo "  restart  — restart the admin web interface"
        echo ""
        echo "  Admin interface runs at http://192.168.1.254:8080 when active."
        exit 1
        ;;
    esac
    ;;

IMPORTANT: The exec pattern (exec sudo systemctl ...) is consistent with how other subcommands work in this dispatcher. When $has_help is true, the exec is bypassed and the function exits. The exec replaces the shell process so there's no return after exec — this is correct.

After adding the case block, the file structure should be:
  CMD="${1:-}"
  shift || true
  ... has_help detection ...
  case "$CMD" in
    status) ... ;;
    watch) ... ;;
    rollback) ... ;;
    routing) ... ;;
    update) ... ;;
    admin) ... ;;    <- new
    *)
      echo "Usage: splitgate {status|watch|rollback|routing|update|admin} [args...]"
      ...
    ;;
  esac
  </action>
  <verify>
    <automated>cd /Users/antonromankov/Projects/my-projects/splitgate && bash -n src/scripts/splitgate && grep -q "admin)" src/scripts/splitgate && grep -q "splitgate-admin.service" src/scripts/splitgate && grep -q "status|watch|rollback|routing|update|admin" src/scripts/splitgate && echo "dispatcher OK"</automated>
  </verify>
  <done>
  - [ ] src/scripts/splitgate passes bash -n (no syntax errors)
  - [ ] admin) case block present with status/start/stop/restart sub-cases
  - [ ] Each sub-case uses exec sudo systemctl {action} splitgate-admin.service
  - [ ] /? help text present for each sub-case
  - [ ] *)  catch-all includes "admin" in the Usage line
  - [ ] All existing subcommands (status, watch, rollback, routing, update) unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Update README.md, docs/REFERENCE.md, docs/README.ru.md, and .planning/STATE.md</name>
  <files>README.md, docs/REFERENCE.md, docs/README.ru.md, .planning/STATE.md</files>
  <action>
Read each file before editing to understand current content length and structure. Phase 11 trimmed README.md to ~150 lines with 3 sections. Keep updates additive and concise.

--- README.md ---

Add a new section "## Web Admin Interface" near the end of the file (before any "---" separator or at the end). Content:

  ## Web Admin Interface

  A browser-based admin UI runs on the RPi at **http://192.168.1.254:8080**.

  **Deploy:**
  ```bash
  cd src/admin && npm run build && cd ../..
  bash src/deploy.sh
  ```

  **Access:** Open http://192.168.1.254:8080 from any LAN device. Enter the admin password when prompted.

  **Default password:** `admin` — change it on the Settings page or:
  ```bash
  echo 'NEWPASSWORD' | ssh pi4 "sudo tee /etc/splitgate/admin.secret"
  ```

  **Pages:**
  - **Dashboard** — VPN tunnel status, daemon state, RU list age, route counts (auto-refreshes 10s)
  - **Services** — start/stop/restart awg0, splitgate-watch, networking, dnsmasq
  - **Routes** — add/remove CIDRs in vpn-routes-custom.txt and isp-routes-custom.txt; Apply button reloads routes
  - **Logs** — Watch Live (real-time SSE stream), Install Log, Watch Errors, System Journal
  - **Config** — edit ru-list-exclude.txt; trigger manual RU list refresh
  - **Settings** — edit /etc/splitgate/vpn-gateway.env and AWG config; change admin password; Rollback

  **CLI management:**
  ```bash
  splitgate admin status    # check if admin service is running
  splitgate admin start
  splitgate admin stop
  splitgate admin restart
  ```

--- docs/REFERENCE.md ---

Add a new top-level section "## Phase 15: Web Admin Interface" (append at the end of the file). Content:

  ## Phase 15: Web Admin Interface

  Flask backend (`/usr/local/bin/splitgate-admin`) serves a React SPA from `/etc/splitgate/admin/`.
  Runs as root under systemd (`splitgate-admin.service`) on port `ADMIN_PORT` (default 8080).
  Auth: HTTP Basic Auth; password stored in `/etc/splitgate/admin.secret` (plain text, mode 600).

  ### Configuration

  | Variable | Location | Default | Description |
  |----------|----------|---------|-------------|
  | ADMIN_PORT | /etc/splitgate/vpn-gateway.env | 8080 | Flask listen port |

  ### Files on RPi

  | Path | Description |
  |------|-------------|
  | /usr/local/bin/splitgate-admin | Flask backend script |
  | /etc/splitgate/admin/ | React SPA built dist (index.html + assets/) |
  | /etc/splitgate/admin.secret | Admin password (plain text, mode 600 root:root) |
  | /etc/systemd/system/splitgate-admin.service | systemd unit |
  | /etc/splitgate/logs/admin-error.log | Flask stderr log |

  ### API Endpoints

  All endpoints require HTTP Basic Auth. JSON request/response unless noted.

  #### Dashboard

  | Method | Path | Description | Response |
  |--------|------|-------------|----------|
  | GET | /api/status | Gateway status overview | {tunnel_up, daemon_up, ru_list_updated, vpn_route_count, isp_route_count, ru_route_count} |

  #### Services

  | Method | Path | Description | Response |
  |--------|------|-------------|----------|
  | GET | /api/services | List service states | [{name, status}] — status: active/inactive/failed/unknown |
  | POST | /api/services/{name}/{action} | Control service | 200 {ok:true} or 500 {error} — action: start/stop/restart; names: awg0/splitgate-watch/splitgate-admin/networking/dnsmasq |

  #### Routes

  | Method | Path | Description | Body / Response |
  |--------|------|-------------|-----------------|
  | GET | /api/routes/vpn | List VPN force-routes | {routes: [cidr, ...]} |
  | POST | /api/routes/vpn | Add VPN CIDR | {cidr: "x.x.x.x/n"} — 201 ok, 400 invalid, 409 duplicate |
  | DELETE | /api/routes/vpn | Remove VPN CIDR | {cidr: "x.x.x.x/n"} — 200 ok |
  | GET | /api/routes/isp | List ISP exception routes | {routes: [cidr, ...]} |
  | POST | /api/routes/isp | Add ISP CIDR | same as vpn |
  | DELETE | /api/routes/isp | Remove ISP CIDR | same as vpn |
  | POST | /api/config/apply | Apply routes (routing.sh --no-update) | 200 {ok:true} or 500 {error} |

  #### Logs

  | Method | Path | Description | Response |
  |--------|------|-------------|----------|
  | GET | /api/logs/watch | Live SSE stream from watch-YYYY-MM-DD.log | text/event-stream — "data: LINE\n\n" |
  | GET | /api/logs/install | Last 500 lines of install.log | {lines: [...]} |
  | GET | /api/logs/watch-errors | Last 200 lines of watch-error.log | {lines: [...]} |
  | GET | /api/logs/journal | journalctl -u splitgate-watch -u awg0 -n 200 | {lines: [...]} |

  #### Config

  | Method | Path | Description | Body / Response |
  |--------|------|-------------|-----------------|
  | GET | /api/config/exclude | Read ru-list-exclude.txt | {content: str} |
  | PUT | /api/config/exclude | Write ru-list-exclude.txt | {content: str} |
  | POST | /api/config/update | Run update-vpn-routes | 200 {ok, output} or 500 {error} |

  #### Settings

  | Method | Path | Description | Body / Response |
  |--------|------|-------------|-----------------|
  | GET | /api/settings/env | Read vpn-gateway.env (secrets masked) | {vars: {KEY: value}} |
  | PUT | /api/settings/env | Write vpn-gateway.env | {vars: {KEY: value}} |
  | GET | /api/settings/secrets | Read awg0.conf key-value pairs (keys masked) | {vars: {Key: value}} |
  | PUT | /api/settings/secrets | Update awg0.conf key-value pairs | {vars: {Key: value}} |
  | POST | /api/settings/password | Change admin password | {password: str} |
  | POST | /api/settings/rollback | Run vpn-rollback.sh | {confirmation: "ROLLBACK"} — 200 initiated or 400 wrong confirmation |

  ### CLI

  ```bash
  splitgate admin status    # check service state
  splitgate admin start     # start admin service
  splitgate admin stop      # stop admin service
  splitgate admin restart   # restart admin service
  splitgate admin /?        # help
  ```

  ### Developer Workflow

  ```bash
  # Build React SPA (run on macOS dev machine)
  cd src/admin && npm install && npm run build

  # Deploy (must build first — deploy.sh skips admin stages if src/admin/dist/ missing)
  cd .. && bash src/deploy.sh

  # First-time password setup
  echo 'SECURE_PASSWORD' | ssh pi4 "sudo tee /etc/splitgate/admin.secret && sudo chmod 600 /etc/splitgate/admin.secret"
  ```

--- docs/README.ru.md ---

Add the Russian translation of the README.md "Web Admin Interface" section in the same position as in README.md. Keep the same structure but translate all prose. Code blocks and URLs remain in English.

  ## Веб-интерфейс администратора

  Браузерный интерфейс управления запущен на RPi по адресу **http://192.168.1.254:8080**.

  **Деплой:**
  ```bash
  cd src/admin && npm run build && cd ../..
  bash src/deploy.sh
  ```

  **Доступ:** Откройте http://192.168.1.254:8080 с любого устройства в локальной сети. Введите пароль администратора.

  **Пароль по умолчанию:** `admin` — сменить на странице Settings или командой:
  ```bash
  echo 'НОВЫЙ_ПАРОЛЬ' | ssh pi4 "sudo tee /etc/splitgate/admin.secret"
  ```

  **Страницы:**
  - **Dashboard** — статус VPN-туннеля, состояние демона, возраст RU-списка, количество маршрутов (авто-обновление 10 с)
  - **Services** — запуск/остановка/перезапуск awg0, splitgate-watch, networking, dnsmasq
  - **Routes** — добавление/удаление CIDR в vpn-routes-custom.txt и isp-routes-custom.txt; кнопка Apply перезагружает маршруты
  - **Logs** — Watch Live (SSE-стрим в реальном времени), Install Log, Watch Errors, System Journal
  - **Config** — редактирование ru-list-exclude.txt; ручное обновление RU-списка
  - **Settings** — редактирование /etc/splitgate/vpn-gateway.env и конфигурации AWG; смена пароля; Rollback

  **Управление через CLI:**
  ```bash
  splitgate admin status    # проверить, запущен ли сервис
  splitgate admin start
  splitgate admin stop
  splitgate admin restart
  ```

--- .planning/STATE.md ---

Read the current STATE.md. Append a new Phase 15 decisions block at the end (or within the appropriate decisions section if one already exists). Content:

  ## Phase 15: Web Admin Interface

  **Status:** Complete (planned 2026-06-30)

  ### Decisions

  - D-01: React + Vite SPA (not vanilla JS, not embedded HTML in Python)
  - D-02: Built dist committed to repo — no Node.js on RPi, no build step at deploy time
  - D-03: src/admin/ — React project source; src/admin/dist/ — committed build output
  - D-04: Flask serves src/admin/dist/ as static files; SPA routing via React Router (hash mode)
  - D-05: deploy.sh checks for src/admin/dist/ before deploying admin UI; if absent: skip with log message
  - D-06: New conditional Stage 29 in deploy.sh for admin (after existing Stage 28 splitgate-watch)
  - D-07: Admin files deploy to /etc/splitgate/admin/ on RPi; Flask backend at /usr/local/bin/splitgate-admin
  - D-08: Python Flask (src/scripts/splitgate-admin.py) — single file, no external Python deps beyond Flask
  - D-09: Auth: HTTP Basic Auth, password stored at /etc/splitgate/admin.secret (plain text, mode 600)
  - D-10: Port: ADMIN_PORT=8080 in .env; accessible at http://192.168.1.254:8080 from LAN
  - D-11: Flask runs as root (systemd User=root) — required for systemctl and /etc/splitgate/ writes
  - D-12: Services page button state: Start disabled when active; Stop and Restart disabled when inactive
  - D-13: Logs page: 4 tabs — Watch Live (SSE), Install Log, Watch Errors, System Journal; [VPN]=cyan [ISP]=yellow
  - D-14: Settings page: edits vpn-gateway.env and awg0.conf; mask secret values; modal for Rollback

  ### File Layout (RPi)

  - /usr/local/bin/splitgate-admin — Flask backend
  - /etc/splitgate/admin/ — React SPA dist (served as static)
  - /etc/splitgate/admin.secret — admin password (mode 600, root:root)
  - /etc/systemd/system/splitgate-admin.service — systemd unit
  - /etc/splitgate/logs/admin-error.log — Flask stderr

  ### Key Packages

  - python3-flask (apt) — Flask on RPi
  - vite + react + react-dom + react-router-dom (npm, dev only — dist committed to repo)
  </action>
  <verify>
    <automated>cd /Users/antonromankov/Projects/my-projects/splitgate && grep -q "Web Admin Interface\|Веб-интерфейс" README.md && grep -q "Web Admin Interface" docs/REFERENCE.md && grep -q "/api/status" docs/REFERENCE.md && grep -q "Веб-интерфейс" docs/README.ru.md && grep -q "Phase 15" .planning/STATE.md && grep -q "D-01\|D-02" .planning/STATE.md && echo "docs OK"</automated>
  </verify>
  <done>
  - [ ] README.md has "## Web Admin Interface" section with access URL, default password, pages list, CLI commands
  - [ ] docs/REFERENCE.md has "## Phase 15: Web Admin Interface" with full API endpoint table (all 17+ endpoints)
  - [ ] docs/README.ru.md has Russian translation of the web admin section
  - [ ] .planning/STATE.md has Phase 15 decisions D-01 through D-14 recorded
  - [ ] All code blocks in docs use correct commands (npm run build, splitgate admin status, etc.)
  - [ ] No stale references to old plan structure or "DRAFT" language remain in any updated file
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| src/scripts/splitgate (bash) → systemctl | exec sudo systemctl — requires sudo privileges on RPi |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-19 | Tampering | splitgate admin subcommand injection | mitigate | Admin sub-action validated against explicit case statement (status/start/stop/restart only); unknown actions fall to *)  exit 1 |
| T-15-20 | Information Disclosure | Admin password in README docs | mitigate | README only documents the DEFAULT password concept — no real secrets documented |
| T-15-SC | Tampering | npm/pip installs | mitigate | No installs in Plan 04; documentation-only changes |
</threat_model>

<verification>
```bash
cd /Users/antonromankov/Projects/my-projects/splitgate
bash -n src/scripts/splitgate                     # must exit 0
grep -c "admin)" src/scripts/splitgate            # expect 1+
grep "status|watch|rollback|routing|update|admin" src/scripts/splitgate  # expect updated usage line
grep -c "/api/" docs/REFERENCE.md                 # expect 15+
grep "ADMIN_PORT" .planning/STATE.md              # expect 1+
```

End-to-end test on RPi (after Phase 15 complete deploy):
```bash
ssh pi4 "splitgate admin status"   # expect: active
ssh pi4 "splitgate admin /?"       # expect: usage text with status/start/stop/restart
```
</verification>

<success_criteria>
- splitgate admin subcommand controls splitgate-admin.service with status/start/stop/restart
- splitgate admin /? shows help text
- Unknown admin sub-action exits 1 with usage
- Usage line in *) catch-all includes "admin"
- README.md web admin section present with URL, password, pages, CLI
- docs/REFERENCE.md has complete API endpoint table for all 17+ endpoints
- docs/README.ru.md translated
- .planning/STATE.md records all 14 Phase 15 decisions
</success_criteria>

<output>
Create `.planning/phases/15-web-admin-interface/15-04-SUMMARY.md` when done
</output>
